# AGENTS.md — Lean4Practice

> 本文件是本项目的**长期记忆与协作契约**。
> 目的：即使历史会话被删除、或换到全新会话/新模型，只要读本文件，就能按同样的方式接续开发，不必重新摸索，也不该重复犯同样的错误。
> **本文件由 AI 助手在恰当时机主动维护**（见 §9），用户只负责交流与拍板。
>
> ⚠️ **子目录约定（重要）**：本项目的一些子目录**可能自带 `AGENTS.md`**。
> **若某个子目录下存在该文件，则那个子目录里的内容由它约束——动手前先读它。**
> 本文件**有意不列举**具体是哪些子目录（见 **D13**）：这是为了让子目录的内容与本文件相互独立，主项目无需也不应知道它们的细节。

---

## 1. 项目概述与当前状态

**Lean4Practice** 是一个 Lean 4 学习与实践项目。

**当前状态（截至 2026-09-19）**：

- 已是 **git 仓库**（分支 `main`），远程 `origin` = `git@github.com:chaoskey/Lean4Practice.git`（SSH），**已推送成功**。
- GitHub 仓库为 **PUBLIC**：<https://github.com/chaoskey/Lean4Practice>（默认分支 `main`）。
- **已建立最小可编译基线**：Lean `v4.34.0` 已安装，`lake build` **通过**（见 D7）。
- **已开始正式学习**：跟练 **TPIL**，**从第 1 章起、一节不跳**（原计划从第 3 章起，已因「完全看不懂」推翻，见 D9）。**人类做题、AI 批改**。学习入口：[`TPIL/进度.md`](./TPIL/进度.md)；路线图 [`TPIL/学习计划.md`](./TPIL/学习计划.md)。
- **学习进度**：截至 2026-09-19 —— 第 2 章 **2-1 到 2-6 已完成（均满分）**；**下一课 2-7（变量与区段：`variable` / `section`）材料尚未发**。逐课进度一律以 [`TPIL/进度.md`](./TPIL/进度.md) 为准。
- **项目性质**：**「AI 主导、人类习作」**——**不再是「完全由 AI 开发」**（见 D9）。
- **有意不引入 mathlib**（原因见 D2/D7；TPIL 后续章节可能需要，届时再定）。
- 已固化的内容：**环境事实**（§2）、**关键决策**（§3）、**协作约定**（§5、§8、§9）；命名与证明风格等约定仍待形成（见 §7）。

> ⚠️ 给接续者的第一条指令：**不要凭想象补全本项目的内容约定**。若本文件某处仍标为「待形成」，说明它尚未确定；请先与用户确认，再把结论写进 §7。

---

## 2. 环境事实（已实测验证，勿重新摸索）

| 项 | 事实 |
|---|---|
| 系统 | WSL2 + Ubuntu 22.04.5 LTS（kernel `6.6.87.2-microsoft-standard-WSL2`） |
| 项目路径 | `/mnt/e/DSHSpace/Lean4Practice`（Windows `E:\DSHSpace\Lean4Practice`） |
| 挂载类型 | `9p`/drvfs：`E:\ on /mnt/e type 9p (rw,noatime,...,msize=65536,...)` |
| 大小写 | **不敏感**（Windows 语义）——实测 `.casetest_ABC` 与 `.casetest_abc` 视为同一文件 |
| 权限位 | **无效**——实测 `chmod 700` 后 `stat` 仍为 `777` |
| 符号链接 | **可用**——实测在 `/mnt/e` 下创建指向 Linux 家目录的符号链接，可创建且可正常读取 |
| 用户家目录 | `~/DSHSpace` 是**符号链接** → `/mnt/e/DSHSpace`（实测两者 **inode 完全相同**，是同一个项目，**勿误判为两个**）。用户在交互终端里 `lake` 可直接使用（PATH 已配好），AI 的 `bash -c` 才需要 `export PATH` |
| CPU / 内存 | **2 核 / 3.8 GiB**（实测可用约 2.2 GiB，空闲常常只有几百 MiB） |
| 磁盘 | Linux 根 `/` 可用约 880 G；`/mnt/e` 可用约 363 G |
| Lean 工具链 | **已安装**：`leanprover/lean4:v4.34.0`，位于 `~/.elan/toolchains/leanprover--lean4---v4.34.0`；`lean`/`lake` 是 elan 的代理可执行文件 |
| elan | `4.2.4`，装于 `~/.elan`（Linux 侧，符合 D1） |
| PATH | ⚠️ elan 只把 `~/.elan/bin` 写进 `~/.profile`，**非登录 shell 不加载** → 见 §6.6 |
| 网络代理 | **可用**：宿主机 WSL2 网关 + 端口 **10808**，用于网络不稳时兜底 → 用法与实测结论见 §6.14 |
| git | `2.34.1`；`user.name=chaoskey`，`user.email=joistwang@sina.com`；本仓库已设 `core.autocrlf=false` |
| GitHub 认证 | **SSH 可用**（`ssh -T git@github.com` 认证为 `chaoskey`）；`gh` CLI 2.92.0 **已登录**，scopes `gist, read:org, repo`，`git_protocol=ssh` |
| gh token 位置 | **明文**存于 `~/.config/gh/hosts.yml`（`gh auth login` 已警告）。属敏感文件，不要提交、不要外传、不要贴进日志 |
| 换行符 | 由 `.gitattributes` 统一托管为 **LF**（见 D4） |

**工具链已就绪，`lake build` 实测通过**（见 D7）。若 `lake` 报 command not found，那是 PATH 问题而非未安装（§6.6）。

---

## 3. 关键决策记录（ADR 风格）

### D1. 源码放 `/mnt/e`，构建缓存与工具链放 Linux 侧 ✅已定并实测验证

- **背景**：`/mnt/e` 是 9p 网络式挂载，读写慢；且**权限位无效、大小写不敏感**。Lean 工具链二进制依赖可执行权限位，`.lake` 构建产物量大且读写频繁。
- **决策**：项目源码留在 `/mnt/e`（便于 Windows 侧直接查看/编辑）；**elan 工具链装到 `~/.elan`（Linux 侧）**；**`.lake` 构建缓存通过符号链接落到 Linux 侧**。
- **依据**：符号链接可用性已实测（§2）。目标目录示例：`~/.lean4-build/Lean4Practice/.lake`。
- **预期做法**（工具链装好后执行）：
  ```bash
  mkdir -p ~/.lean4-build/Lean4Practice
  ln -s ~/.lean4-build/Lean4Practice/.lake /mnt/e/DSHSpace/Lean4Practice/.lake
  ```
- **✅ 验证结果（2026-09-17 实测）**：`.lake` 已建为符号链接 → `~/.lean4-build/Lean4Practice/.lake`。oleans 实体经 `df` 确认落在 **Linux 根文件系统**（挂载点 `/`）；经符号链接读写正常；`git status` 中 `.lake` 被正确忽略。**该决策成立，勿再怀疑。**
- **约束**：`.lake` 已写入 `.gitignore`，不得提交。

### D2. 不运行无缓存的全量构建 ⚠️硬约束

本机仅 **2 核 / 3.8 GiB 内存**。**从源码构建 mathlib 在本机基本不可行**（内存与核数都不够，且可能拖垮 WSL）。

- 需要 mathlib 时，**必须优先使用预编译缓存**（如 `lake exe cache get`），不要 `lake build` 整个 mathlib。
- 大构建前提示用户关闭其他占用内存的程序；同一时间避免并行跑多个重构建。
- 若某任务确需全量构建，**先向用户说明代价并取得同意**，不要闷头开跑。

### D3. 项目定位边学边定 ✅已定

不预先假设「跟教材 / 形式化 / 建库」中的某一条；实际形态由进展决定，并**及时回写本文件**。

### D4. 版本控制：分支 `main`，SSH 远程，换行符由 `.gitattributes` 托管 ✅已定

- **背景**：`core.autocrlf` 未设置，项目又位于 Windows/WSL 共享盘，双向编辑极易改动换行符、污染 diff（原 §6.5 风险）。
- **决策**：仓库默认分支为 `main`；仓库内设 `core.autocrlf=false`；**提交 `.gitattributes`，把文本文件显式固定为 LF**，二进制类型显式标记为 `binary`。
- **远程**：`origin` 走 SSH（`git@github.com:chaoskey/Lean4Practice.git`），不用 HTTPS，避免推送环节 token 落盘。
- **仓库可见性**：现为 **PUBLIC**。创建时曾是 PRIVATE，**用户于 2026-09-17 主动改为公开，并确认属有意为之**。改回私有的命令（如需）：
  ```bash
  gh repo edit chaoskey/Lean4Practice --visibility private
  ```
- **⚠️ 仓库已公开：不得写入敏感信息**。本文件记录了本机环境细节（路径、内存、WSL 网关形态、`gh` token 的**存放位置**等）——这些属于可公开的一般性信息，但**今后严禁向仓库加入任何凭据、token、私钥、内网地址、他人隐私**。**不确定某内容是否适合公开时，先问用户，不要先提交再问。**
- **⚠️ 不得写入与本项目无关的内容**（用户 2026-09-17 明确要求）：**不要把用户其他项目的信息带进本文件**——本项目是公开仓库，与本项目无关的项目名称、路径、状态都属于不该外泄的内容。**需要保留某条约定的，就把它作为本项目自己的约定写下来，不要写成「从别处沿用」。**

### D5. `README.md` 的对外人机分工声明 ✅已定（后经 D9 修订）

- **来源**：用户明确要求——除常规项目说明外，**特意强调项目的 AI 属性**。
- **决策（初版）**：`README.md` 顶部以「徽章 + 引用块横幅」双重强调「完全由 AI 开发」，并单设章节写明分工。
- **⚠️ 已由 D9 修订**：项目改为「**AI 主导、人类习作**」后，徽章、「分工」表与「须知」**全部改写**——因为人类开始亲自写习题证明，「完全由 AI 开发」不再成立。**以 D9 与 `README.md` 的实际内容为准，本节仅存沿革。**
- **必须始终保留的诚实条款**：README 中保留「AI 输出可能出错，『AI 说已验证』不等于真的验证过」。这是该声明可信度的基础，**不要为了让项目看起来更可靠而删掉这一段**。
- **语言**：中文（遵循 §5.1）。

### D6. 采用 MIT 许可证 ✅已定

- **来源**：用户明确指定「采用 MIT 许可」。
- **决策**：仓库根目录放**标准 MIT 全文** `LICENSE`，`Copyright (c) 2026 chaoskey`。
- **版权主体**：归 **GitHub 账号 `chaoskey`**（自然人）。本项目内容虽全部由 AI 生成，但**在多数司法辖区 AI 不能成为著作权主体**，故权利归创建并维护仓库的人。该主体是用户未指定时由助手按惯例选定的默认值；**若要改成真实姓名或邮箱，只需修改 `LICENSE` 第 3 行**。
- **README**：已加 MIT 徽章并改写「许可」章节，同时说明版权归属；该徽章 URL 为纯 ASCII，无需百分号编码（对比 §6.12）。
- **⚠️ 不要改动许可证措辞**：`LICENSE` 是法律文本，**不要翻译、改写或增删**；如需更换许可证，应整体替换为标准全文。

### D7. 最小可编译基线已建立（Lean v4.34.0，不含 mathlib） ✅已定并已验证

- **来源**：用户选择「先建立最小可编译基线」，而不是直接引入 mathlib、也不是先定方向。
- **决策**：工具链固定 **`leanprover/lean4:v4.34.0`**（写入 `lean-toolchain`）；**暂不引入 mathlib**——本机内存吃紧（D2），需要时再定，改 `lean-toolchain` 一行即可。
- **项目骨架**：`lakefile.toml`（包名 `lean4practice`，库名 `Lean4Practice`）+ 库根模块 `Lean4Practice.lean` + `Lean4Practice/Basic.lean`（冒烟测试）。
- **实测数据**：首次 `lake build` **25 秒**通过；增量构建 **0 秒**；`.lake` 体积约 **100 K**（纯核心、无依赖）。
- **验证强度（重要）**：除「构建成功」外还做了**反向测试**——故意加入错误证明 `(1 : Nat) = 2 := rfl`，确认构建**确实失败**并报出类型错误，随后还原并重新构建成功。这排除了「构建假成功」，说明工具链真的在做类型检查。
- **⚠️ 骨架是临时的**：库名与目录结构是为跑通链路而设，**主线形态定下后可能调整**（见 §7）。

### D8. git 提交必须由用户主动发起 ⚠️硬约束 ✅已定

- **来源**：用户明确要求——「以后关于 git 的提交，始终应该有我主动独立发起请求」。
- **决策**：**AI 不得自行 `git commit`，也不得自行 `git push`**，必须等用户明确提出后才执行。
- **适用范围（无例外）**：包括文档同步、踩坑记录、`AGENTS.md` 的自我维护、改错别字在内，**一律适用**。「文件改了所以顺手提交」**不构成提交理由**——本条修正了 §9 原有的隐含许可。
- **AI 仍可自由修改文件**：工作区改动不受此限制。改完后必须**明确列出待提交内容**，可以**建议**提交信息，但**何时提交由用户决定**。
- **粒度**：只做用户明确说的那一步——说「提交」就只 `commit`；说「提交并推送」才 `push`；**没把握就先问**。
- **理由**：用户要保留对版本历史的完全控制权，避免 AI 以「顺手」为名制造大量未经审阅的提交。

### D9. 学习模式：人类写证明、AI 批改；跟练 TPIL ✅已定

- **来源**：用户选择「**你写证明，我批改**」与「证明入门（**TPIL**）」。
- **决策**：
  - **人类负责写习题证明**；AI **不得代做**。AI 可以做的是：讲解、给**示范**（必须用与习题**不同**的例子）、给提示、**批改**并指出思路缺口。
  - 教材固定为 **Theorem Proving in Lean 4（TPIL）**。
  - **⚠️ 起止点已修订（2026-09-17）**：**改为从第 1 章起、一节不跳**。
    原判断「第 1 章是介绍、第 2 章偏理论，直接从第 3 章开始」**是错的**——用户卡住的概念（`Prop` vs `Type`、`Nat` 是什么、`#check` 输出怎么读）**全在第 2 章 2.1 / 2.2**，跳过一整章地基的结果是**第 3 章完全看不懂**。
    **教训：「这一章看起来只是理论」不等于可以跳过。**
  - 第 2 章拆成 **10 课**（原为 9 课）：因 2.1 一节含两类完全不同的内容（工具 vs 概念），按「一课只引入一个新概念」拆开。路线图见 [`TPIL/学习计划.md`](./TPIL/学习计划.md)。
  - 旧的第 3 章材料（`03-*`）**暂不启用**，待学到第 3 章时按新方式重做。
  - 官方阅读顺序（实测自官网导航，**与字母序不同**）：
    Introduction → Dependent Type Theory → **Propositions and Proofs** → Quantifiers and Equality → Tactics → Interacting with Lean → Inductive Types → Induction and Recursion → Structures and Records → Type Classes → Conv → Axioms and Computation。
- **⚠️ 这改变了项目性质**：本项目**不再是「完全由 AI 开发」**，而是「**AI 主导、人类习作**」。`README.md` 的徽章、「分工」与「须知」均已同步改写。
- **目录约定**：`TPIL/` 放学习材料，**有意不纳入构建库**（不 import 进 `Lean4Practice.lean`），以免一份未完成的习题让整个 `lake build` 变红；改为**逐文件检查**：
  - `进度.md` —— 进度、卡点、验收命令（**新会话先读这个**）
  - `NN-讲义.md` —— AI 写的讲解
  - `NN-示例.lean` —— AI 写的示范，**必须全部编译通过且零 warning**
  - `NN-习题.lean` —— **人类作答**，初始为 `sorry` 占位
- **给 AI 的硬性要求**：布置习题前**必须自己先私下写一份参考解并编译验证**，确认**每题都有解**——**不得布置无解的题**。参考解**不落入仓库**（避免被提前看到），仅用于自检与批改。

### D10. git 提交时的代理约定（用户指定）✅已定

- **来源**：用户给出的流程，并**两次强调收尾必须清代理**——**先配代理 → 提交/推送 → 务必清代理**：
  ```bash
  # ① 判定环境并取代理 IP
  host_ip=$(ip route show default | awk '{print $3}')   # WSL2：取宿主 IP
  #    若在 Win10 原生环境执行，则直接用 127.0.0.1

  # ② 配代理
  git config --global http.proxy  "http://${host_ip}:10808"
  git config --global https.proxy "http://${host_ip}:10808"

  # ③ 提交 / 推送 ……

  # ④ 务必清代理（用户强调）
  git config --global --unset http.proxy
  git config --global --unset https.proxy
  ```
- **⚠️ 生效范围（2026-09-17 实测，勿想当然）**：`http.proxy`/`https.proxy` **只作用于 HTTP(S) 远程，对 SSH 远程完全无效**：
  - 实测 A：代理指向必然不可达的 `127.0.0.1:9`，走 **SSH** 远程 `ls-remote` → **仍然成功**（exit 0）
  - 实测 B：同一不可达代理，走 **HTTPS** 远程 → **失败**（exit 128 `Connection refused`）
  - 同配置换真代理访问 HTTPS → 成功（exit 0），证明配置本身有效
  故：**本仓库 `origin` 是 SSH，这套配置对它的 push 不起作用**。本项目 push 若需走代理，用 §6.14 的 **`ProxyCommand`** 法；本节这套留给 HTTPS 远程。
- **为什么「务必清代理」是硬要求（不只是整洁）**：它是 `--global`，**影响本机所有仓库**；而 `host_ip` 是**动态的**（WSL 重启后网关会变）。一旦代理配置留在全局且该 IP 已失效，**所有 git HTTP(S) 操作都会失败**，且报错指向 `127.0.0.1` 或旧网关，**很难联想到是遗留配置**。所以收尾必须 unset，并**复查**：
  ```bash
  git config --global --get http.proxy  || echo "已清理"
  git config --global --get https.proxy || echo "已清理"
  ```
- **测试这类配置时不要写进全局**：用 `git -c http.proxy=... <命令>` 内联即可，语义相同且**不残留**（本次实测即用此法，事后复查确认全局未设置）。
- **本项目的默认路径仍是直连**：历次 push 均直连成功；代理只在网络不稳时启用。

### D11. 讲义必须自包含中文；英文原文仅供参考 ✅已定

- **来源**：用户反馈「读英文原文还是有点吃力」，并明确要求「尽量用中文来讲解」（2026-09-17）。
- **决策**：所有 `NN-K-讲义.md` **必须自包含**——**只读讲义就能完整学会本课，不得把「读懂英文原文」设为前提**。
  - 讲义里可以（也应该）**附原文链接**，但那只是「想深挖时的延伸阅读」，**不能成为理解本课的必要条件**。
  - 关键概念、术语、例子**一律用中文讲透**；必要术语给「中文（English）」对照，方便日后查英文资料。
  - 遇到只有英文才说得清的地方，**我负责翻译并解释**，而不是把原文推给用户。
- **反面例子（勿犯）**：只写「详见原文 2.3 节」而不解释内容——对用户等于没讲。
- **理由**：用户是中文母语者，英文原文是额外负担而非助力；**讲义存在的意义正是消除这个负担**。
- **推论**：讲义要写得比原文**更细、更慢**，而不是更简略——因为它是唯一的入口。
- **适用范围不止讲义**（用户 2026-09-17 再次强调「尽量用中文来**交流**」，故扩大）：
  **回复、讲义、代码注释、提交信息、文档——一律中文。**
  唯一例外是 **Lean 的语法关键字**（`def`、`#check`、`theorem`、`Type`、`Prop`…）**必须照写英文**，
  因为 Lean 只认这些拼写；但**每次出现都要用中文讲清它是什么意思**，不能默认用户认识。
- **自检**：写完一段材料后问自己——**一个只懂中文、没读过英文原文的人，能看懂吗？** 不能就是没写完。

### D12. 检查脚本 `TPIL/check.sh`（纯 ASCII 调用）✅已定

- **来源**：用户**手敲含中文的课文件名会字符错位**——实测把 `lake env lean TPIL/02-1-示例.lean` 敲成了 `lake env lea` + `TPIL/02-1-示例.lean` + `an`，于是 Lean 去找 `TPTPIL/...` 并报「找不到文件」。用户同意加一个用纯 ASCII 参数的检查脚本。
- **用法**：
  ```bash
  bash TPIL/check.sh 02-2      # 课号是纯 ASCII，不用碰中文文件名
  ```
- **行为**：找出 `TPIL/<课号>-*.lean` → 逐个 `lake env lean` → 报告退出码 / 警告数 / 错误数
  - **示例**类文件：要求**零警告零错误**，否则标 ⚠️
  - **习题**类文件：有编译错误 → ❌；否则统计「整行就是 `sorry`」的占位数——`0` → ✅ 验收通过，`>0` → ⏳ 还没做完
- **⚠️ 退出码语义是刻意设计的**：**「习题还没做完」不算失败**（那只是正常的中间状态），只有**真问题**（编译错误、或示例文件出现警告）才返回非 0。这样脚本可以放心用在自动化里。
- **为什么写 `bash TPIL/check.sh` 而不是 `./TPIL/check.sh`**：`/mnt/e` 上**权限位无效**（见 §6.2），不能依赖可执行位；显式用 `bash` 调用最稳。
- **脚本会自动 `cd` 到项目根**（`$(dirname "$0")/..`），所以**从任何目录调用都可以**。
- **已实测的 5 种情形**：已完成的课（✅ 验收通过）、未完成的课（⏳ 还有 N 处 `sorry`）、无参数（打印用法 + 现有课号列表）、课号不存在（报错 + 列表）、从别的目录调用（正常）。

### D13. 子目录可自带 `AGENTS.md`（子目录自治）✅已定

- **来源**：用户要求的**隔离设计**——某些子目录的内容**不应体现在主项目中**（不进仓库、主文件不提），但那份内容**又需要受到约定约束**。这两件事看似矛盾，用「子目录自带 AGENTS.md」就同时满足了。
- **决策**：本项目**任何子目录都可能自带 `AGENTS.md`**。
  - 处理某个子目录前，**先读该目录下的 `AGENTS.md`（若存在）**；
  - 该子目录内的内容，**由那份文件约束**，本文件不干预其细节。
- **⚠️ 本文件有意不列举哪些子目录带 `AGENTS.md`。** 这是**设计的一部分，不是遗漏**：
  - 主项目**不需要、也不应该**知道那些子目录的细节；
  - 两者**相互独立**：本文件立**通用**规矩，子文件立**局部**规矩。
- **🔴 给接续者的硬性要求**：**不要因为本文件没提到某个子目录，就认为那里没有约定。**
  只要看到某个子目录里存在 `AGENTS.md`，就**必须先读它、并遵守它**。
- **边界（重要）**：子目录的 `AGENTS.md` 是**补充，不是替代**——它**不得推翻本文件的硬约束**，尤其：
  - **D2**（不运行无缓存的全量构建）
  - **D4**（不得写入敏感信息 / 与本项目无关的内容）
  - **D8**（git 提交必须由用户主动发起）
  子文件若与这几条冲突，**以本文件为准**。

### D14. 材料写作三条硬规矩（来自用户三次卡点）✅已定

- **来源**：2026-09-19，用户在 **02-2 一课之内连卡三次**，三次都**不是「内容太难」**，而是**我写材料的方式**有问题：
  1. 用「同上」做**就近指代**，而它字面指向的其实是**另一行**（本意是 `.1`，字面却指向 `.2`）→ 用户看不懂；
  2. 写「两者意思一样」却**没写明哪一点一样**（我指「排版」，用户读成「两个函数」）——而且那句「一样」**本身就是错的**（`Nat.succ` 吃 1 个参数，`Nat.add` 吃 2 个）；
  3. 在示例里用了 `example : a = b := rfl`，而 `example` / `=` / `rfl` **全是第 3 章的内容**，本课一个字都没讲。
- **决策（三条硬规矩，适用于 `NN-K-讲义.md` / `NN-K-示例.lean` / `NN-K-习题.lean`）**：
  1. **禁止「同上」「同理」「如前」这类就近指代**。必须写明**指哪一个具体的表达式/哪一行**（写成「`.fst` 与上面的 `.1` 相同」这种）。
  2. **说「两者一样」必须写明「哪一点一样」**：显示一样？类型一样？值一样？——并且**先确认这句话本身成立**（`Nat.succ` 与 `Nat.add` 就不是「一样」）。
  3. **材料里不得出现本课尚未讲解的语法或概念。** 要么**当场解释清楚**，要么**明确标注**「属于第 N 章，本课不引入，现在不必理解」并给出**不依赖它的**替代说法或验证手段。
- **推论（写完必做的自检）**：材料的**每一行都必须能被「只学过本课及以前各课的人」解释**。逐行自问：**这一行的每个符号，我在哪一课讲过？**
- **配套要求（沿用 D9/D12）**：示例文件**必须零 warning**；讲义里出现的每一段 Lean 输出**都必须实跑核对**（见 §6.3、§7 原则 3）。
- **理由**：三次卡点里**没有一次**是「内容太难」——全部是**表达含混**或**用词越界**。因此：**用户说「看不懂」时的默认判断是「我的材料有问题」**，而不是「用户没看懂」。

### D15. 习题不得泄露答案（题面只给约束与提示；验证区注释也不得写预期输出）✅已定

- **来源**：2026-09-19，用户在批改 02-6 **之前**指出（原话大意：「习题的验证区里，注释中你把答案都写出来了，这是不应该的；前面也有一部分，你在注释中把答案写得很清楚，这也不应该——注释里最多只能有提示」）。**批评成立**，具体两处：
  1. **验证区**：虽然整段被 `--` 注释掉（§6.20 的做法），但**注释文字本身就是答案**——`-- #eval quad 2   -- 16`、`-- #check area  -- area (w h : Nat) : Nat` 之类，学生**只要读文件就看见答案**，「先判断、再核对」形同虚设。
  2. **题面**：前 3 题的「要求」把答案的写法几乎整句拼了出来（「先 `let d := x + x`，再 `let q := d * d`，最后返回 `q`」），学生只剩**照抄**，失去了练手的机会。
- **决策（适用于 `NN-K-习题.lean`）**：
  1. **题面只写「要什么」，不写「怎么写」**：可以给**约束**（「必须用 `let`」「用分号写法、写在同一行」「两个 `let` 各占一行」「类型标注写全」）与**提示**（「提示：讲义第 N 节的那个例子」），**但不得把答案表达式写出来**。
  2. **验证区不得出现答案**：可以放**能跑的自查语句**（`#check` / `#eval`，让学生自己核对），**但注释里不得写预期输出**，也不得让注释本身构成答案；需要指路就写「与讲义第 N 节的哪张表对照」。
  3. **配套自检（出完题必做）**：**假装自己是学生，把习题文件从头读一遍——光看这个文件，能不能直接把答案抄出来？** 能，就是没出好。
- **与 §6.20 的关系**：§6.20 只解决了「**一跑脚本**答案就印在屏幕上」，**没有解决「答案就写在注释里」**；本条补上另一半。**两条要一起满足。**
- **例外（重要）**：`NN-K-示例.lean`（示范）与 `NN-K-讲义.md` **本来就该把内容讲透、把例子写全**，不受本条约束——本条**只管习题文件**。
- **本次处理**：用户明确说「这次就算了」，故 `02-6-习题.lean` **不回头改**；**从 02-7 起按本条执行**。

---

## 4. 目录结构与现状

截至 2026-09-19，仓库内**实际存在**的文件（当前状态：**可编译通过**）：

```
Lean4Practice/
├── README.md            # 对外说明；声明本项目为「AI 主导、人类习作」（D5/D9）
├── LICENSE              # MIT 许可证全文（D6）
├── AGENTS.md            # 本文件（长期记忆与协作契约）
├── lean-toolchain       # 固定 Lean 版本：leanprover/lean4:v4.34.0（D7）
├── lakefile.toml        # Lake 项目定义（包名 lean4practice，库名 Lean4Practice）
├── lake-manifest.json   # 依赖锁定（当前无依赖）；由 lake 生成，**应提交**
├── Lean4Practice.lean   # 库根模块，负责 import 各子模块
├── Lean4Practice/       # 源码目录（目录名须与库名一致）
│   └── Basic.lean       # 冒烟测试：几个 trivial 证明
├── TPIL/                # 学习材料（**有意不纳入构建库**，逐文件检查，见 D9）
│   ├── 学习计划.md       # 路线图（含「从第 3 章重开为第 1 章」的教训）
│   ├── 进度.md           # 当前位置 + 卡点 + 学习记录（**新会话先读**）
│   ├── 01-1-讲义.md      # 第 1 章讲义（纯背景，无 .lean 文件）
│   ├── 02-1-讲义.md      # 第 2 章第 1 课：每个表达式都有类型
│   ├── 02-1-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-1-习题.lean    # 习题（**人类作答**）—— 已完成，6/6 全对
│   ├── 02-2-讲义.md      # 第 2 章第 2 课：从已有类型造新类型（→ 和 ×）
│   ├── 02-2-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-2-习题.lean    # 习题（**人类作答**）—— 已完成，7/7 全对（订正一次）
│   ├── 02-3-讲义.md      # 第 2 章第 3 课：`Prop` 与 `Type`（类型本身也是对象）
│   ├── 02-3-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-3-习题.lean    # 习题（**人类作答**）—— 已完成，7/7 全对（补答一问）
│   ├── 02-4-讲义.md      # 第 2 章第 4 课：函数抽象与求值（`fun`、应用、`#eval`）
│   ├── 02-4-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-4-习题.lean    # 习题（**人类作答**）—— 已完成，8/8 全对（订正三处）
│   ├── 02-5-讲义.md      # 第 2 章第 5 课：定义（`def`）
│   ├── 02-5-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-5-习题.lean    # 习题（**人类作答**）—— 已完成，8/8 全对（订正两处）
│   ├── 02-6-讲义.md      # 第 2 章第 6 课：局部定义（`let`）
│   ├── 02-6-示例.lean    # 示范（AI 写，已验证通过且零 warning）
│   ├── 02-6-习题.lean    # 习题（**人类作答**）—— 已完成，8/8 全对（订正四处）
│   ├── check.sh          # 检查脚本：`bash TPIL/check.sh 02-6`（**纯 ASCII 参数**，见 D12）
│   └── 03-*.md / *.lean  # 【**暂不启用**】旧的第 3 章材料，待按新方式重做
├── .gitignore           # 忽略 .lake / 构建产物 / 编辑器杂项
├── .gitattributes       # 换行符与文本/二进制属性托管（D4）
├── .lake -> ~/.lean4-build/Lean4Practice/.lake   # 符号链接，本机专属，已被忽略
└── .git/                # 本地仓库
```

> 注：`lakefile.toml` 与 `lakefile.lean` **不要同时存在**。
> 注：**新增 `.lean` 文件后，必须在 `Lean4Practice.lean` 里加上对应 `import`**，否则 `lake build` 不会编译它（这是 Lean 与多数语言不同的地方）。
> 注：`TPIL/` 下的文件**故意不 import 进库**——一份未完成的习题不应让整个 `lake build` 变红。它们用 `lake env lean <文件>` 单独检查。
> 注：`.lake` 是**符号链接**，只在本机成立，必须保持被忽略。

---

## 5. 本项目已确立的通用约定

以下约定在本项目中生效：

1. **沟通与文档语言**：与用户交流、以及项目文档，一律使用**简体中文**。
2. **提交信息用中文**：首行简短概括；必要时在正文说明「为什么」。
3. **`AGENTS.md` 是唯一约定入口**：新会话先读本文件，再动手。
4. **文件编码统一 UTF-8**。
5. **不修改原始/权威资料**：只读输入（原始 PDF、参考基线）永不就地改动。
6. **工作产物与最终交付物分离**：如需放脚本与中间产物，用 `work/` 目录。
7. **进展可续**：长流程必须有「进度」记录，并标注 **🔧自动 / 👤人工**。
   本项目已落实为：[`TPIL/学习计划.md`](./TPIL/学习计划.md)（路线图）+ [`TPIL/进度.md`](./TPIL/进度.md)（当前位置与卡点）。
8. **子目录自治**：**任何子目录都可能自带 `AGENTS.md`**；若存在，则该子目录的内容由它约束，**动手前先读它**。
   本文件**有意不列举**是哪些子目录——详见 **D13**。
9. **材料写作三条硬规矩**（讲义 / 示例 / 习题一律适用）：① 禁用「同上」「同理」这类**就近指代**；② 说「两者一样」必须写明**哪一点**一样；③ **不得出现本课尚未讲解的语法或概念**（要么当场解释，要么明确标注「第 N 章再讲」并给出不依赖它的说法）。
   详见 **D14**；违反时的自检问句：**「这一行的每个符号，我在哪一课讲过？」**
10. **习题不得泄露答案**：题面**只写「要什么」+ 约束 + 提示**，验证区**注释里也不得写预期输出**（`.lean` 示例/讲义不受此限）。
    详见 **D15**；自检问句：**「假装自己是学生，光看这个习题文件，能不能直接抄出答案？」**

---

## 6. 踩坑与风险清单（本机实测结论）

按「避免重复踩坑」的目标，**每条都给出可执行的对策**。

1. **大小写不敏感**：`Foo.lean` 与 `foo.lean` 会互相覆盖，模块名不能只靠大小写区分。写文件前先确认命名唯一（忽略大小写）。
2. **权限位无效（恒为 777）**：任何依赖 `chmod +x` 的流程（工具链、脚本）**不要放在 `/mnt/e`**；放 Linux 侧。
3. **9p 挂载慢**：大批小文件读写（构建产物、依赖下载）**必须放 Linux 侧**；不要把 `.lake` 留在 `/mnt/e`。
4. **内存极小**：见 D2。警惕 OOM；重任务前后用 `free -h` 确认余量。
5. **换行符**：本仓库已用 `.gitattributes` 固定为 LF（D4），**该文件不要删**；新加文本类型时记得补进 `.gitattributes`。
6. **`lake`/`lean` 不在 PATH 里**（极易被误判成「工具链没装」）：elan 只把 `~/.elan/bin` 写进 `~/.profile`，而 **本 harness 的每条命令都是非登录、非交互 shell，不会加载 `~/.profile`**，直接调 `lake` 必报 command not found。**每条涉及 Lean 的命令都要先加**：
   ```bash
   export PATH="$HOME/.elan/bin:$PATH"
   ```
   工具链本身**已装好**（§2/D7），不要因这个报错去重装。
7. **符号链接需谨慎提交**：`.lake` 之类的链接只在本机成立，**必须保持被 gitignore**，否则会把本机绝对路径泄漏进仓库。
8. **本项目在 `E:` 盘**：Windows 侧程序可能同时在编辑同一批文件；改动前留意非 WSL 来源的变更，避免互相覆盖。
9. **`/mnt/e` 上 `git` 较慢**：9p 下 `git status`/`add` 大仓库时明显变慢；保持仓库精简，不要把构建产物纳入版本控制。
10. **仓库可见性会改变 API 行为（易误判）**：本仓库**已公开**，匿名 `curl https://api.github.com/repos/chaoskey/Lean4Practice` 现在返回 **200**；而**私有**时同一请求返回 **404**——与「仓库不存在」**完全无法区分**，曾导致误判成「没建成功」。**判断可见性/存在性要用已登录的 `gh`，不要靠匿名请求猜**：
    ```bash
    gh repo view chaoskey/Lean4Practice --json visibility,isPrivate,url
    ```
11. **SSH 无法创建仓库**：GitHub 不支持 push-to-create；远端仓库必须先由网页或 API 创建一次，之后推送才能全走 SSH。不要反复重试 `git push` 试图「创建」仓库。
12. **shields.io 徽章里的非 ASCII 必须百分号编码**：`README.md` 顶部徽章中，直接把中文写进 URL（如 `.../badge/status-早期搭建中-orange`）会返回 **HTTP 400**，页面上显示为**破图**。必须用百分号编码，例如：
    ```
    https://img.shields.io/badge/status-%E6%97%A9%E6%9C%9F%E6%90%AD%E5%BB%BA%E4%B8%AD-orange
    ```
    生成方式：`python3 -c "import urllib.parse;print(urllib.parse.quote('早期搭建中',safe=''))"`。
    **改动徽章后要用 `curl -o /dev/null -w '%{http_code}'` 确认返回 200**，不要凭肉眼判断。
13. **Lake 要求库根模块文件存在**：`lean_lib` 名为 `Foo` 时，Lake 要求存在 **`Foo.lean`**（根模块）；**只有 `Foo/` 目录没有根文件会直接报错** `no such file or directory ... Foo.lean`。新增源码的完整流程是：写 `Lean4Practice/Xxx.lean` → 在 `Lean4Practice.lean` 中 `import Lean4Practice.Xxx`。
14. **网络不稳定，下载必须重试**：本机访问 GitHub 实测会间歇失败——`curl: (16) Error in the HTTP2 framing layer`、`(28) Failed to connect ... timed out`、`(56) SSL_read: unexpected eof`。elan 安装包实测**第 3 次尝试**才成功。**不要把单次失败当成「资源不可用」而放弃或改方案。**
    **兜底顺序**：① 直连 + 重试（并加 `--http1.1`）→ ② 仍失败则挂**宿主机代理**。
    - ⚠️ **本 harness 的 `web_fetch` 工具不走代理**（2026-09-19 实测：取 GitHub 原始文件连续两次 30 秒超时）。**网页取不到时改用 `bash` + `curl` 走代理**，同一内容实测 **HTTP 200** 可取回：
      ```bash
      host_ip=$(ip route show default | awk '{print $3}')
      export http_proxy="http://$host_ip:10808" https_proxy="http://$host_ip:10808"
      curl -sS --http1.1 --max-time 90 -o /tmp/page.html -w '%{http_code}\n' "<URL>"
      ```
    - ⚠️ **别把 404 当成「代理不通」**：实测 `raw.githubusercontent.com/leanprover/theorem_proving_in_lean4/master/Dependent-Type-Theory.lean` 返回 **404**（该仓库里没有这个路径），而同一时刻 `lean-lang.org` 的在线书页面返回 **200**。**先看 HTTP 状态码，再判断是不是网络问题。**
    - ✅ **代理用完即弃、不会残留**（用户 2026-09-19 再次提醒「用完记得清理」）：上面这种 `export` 形式只活在**这一次 `bash` 调用**里（本 harness 每条命令都是独立 shell，已实测复查确认无残留）；**需要手工清理的只有 `git config --global` 形式**（见 D10）。复查命令：
      ```bash
      git config --global --get http.proxy  || echo "已清理"
      git config --global --get https.proxy || echo "已清理"
      ```

    **宿主机代理用法（用户提供，2026-09-17 实测可用）**：
    ```bash
    host_ip=$(ip route show default | awk '{print $3}')   # WSL2 网关 = Windows 宿主机
    export http_proxy="http://$host_ip:10808"
    export https_proxy="http://$host_ip:10808"
    ```
    - 代理端口固定 **10808**；实测 `http://` 与 `socks5h://` 均可用（`curl -x` 形式）。
    - **必须用环境变量形式，不要只用 `curl -x`**：`elan` / `lake` 等自带的下载器**不认命令行 `-x` 参数，只读 `http_proxy`/`https_proxy` 环境变量**（实测环境变量方式 HTTP 200 通过）。
    - ⚠️ **IP 必须动态获取，不要写死**：取它的办法就是上面那行 `ip route show default | awk '{print $3}'`——**每次现取**，因为 WSL 重启后网关地址会变。
      **🔴 不要把具体地址写进仓库**（2026-09-19 用户明确要求）：具体值对本机之外的人毫无意义、而且必然过时；需要地址时**现算**即可。本行原先记录过那个实测值，已删除——**这里也不再重复它**。同理适用于任何内网地址（见 **D4**）。
    - 实测 GitHub release 下载经代理与直连**字节一致**（`cmp` 通过），代理不污染内容。
    - **对 SSH 不生效，但可另行配置（易踩的误区）**：`origin` 走 SSH，而 **SSH 根本不读 `http_proxy`/`https_proxy`**——所以上面那套 `export` **对 `git push` / `git fetch` 完全无效**。若 SSH 也需走代理，**必须用 `ProxyCommand`**（2026-09-17 实测两种均可用）：
      ```bash
      host_ip=$(ip route show default | awk '{print $3}')
      # HTTP CONNECT
      GIT_SSH_COMMAND="ssh -o ProxyCommand='nc -X connect -x $host_ip:10808 %h %p'" git push
      # 或 SOCKS5
      GIT_SSH_COMMAND="ssh -o ProxyCommand='nc -X 5 -x $host_ip:10808 %h %p'" git push
      ```
      实测 `git ls-remote` 经代理返回正常、认证输出 `Hi chaoskey!`（`ssh -T` 退出码为 1 属正常，GitHub 不提供 shell）。
      也可写进 `~/.ssh/config`，但那是**本机配置，不属于仓库**：
      ```
      Host github.com
          ProxyCommand nc -X connect -x <网关IP>:10808 %h %p
      ```
      前提：`nc` 支持 `-X`/`-x`（本机 `/usr/bin/nc` **实测支持**）；`ncat`/`socat` 均未安装。
    - **两条代理路径不要混用**：**HTTP(S) 远程** → 用 `http_proxy` 环境变量或 `git config http.proxy/https.proxy`（见 **D10**）；**SSH 远程 → 只能用上面的 `ProxyCommand`**。混用会让你「以为挂了代理，实际根本没走」。
    - **实测现状（勿默认开代理）**：HTTPS 直连可用（HTTP 200、约 0.4 秒）；**GitHub 的 SSH 直连也一直通**（本项目历次 push 均直连成功）。代理是**不稳定时的兜底**，不是默认路径。
15. **`sorry` 只是 warning，退出码为 0**（会误导「已完成」的判断）：对含 `sorry` 的文件，`lake env lean` **返回 0**——「能通过检查」**不等于**「题做完了」，空壳答案一样能过。**验收必须同时满足两条**：
    ```bash
    export PATH="$HOME/.elan/bin:$PATH"
    lake env lean TPIL/03-习题.lean && ! grep -qE '^[[:space:]]*sorry[[:space:]]*$' TPIL/03-习题.lean
    ```
    即 ① 无编译错误 ② 文件内不再有「**整行就是 `sorry`**」的占位。
    ⚠️ **切勿用 `grep -n 'sorry'` 这种简单写法**——它会把**注释里提到 `sorry` 的文字**也算作占位，导致文件明明做完了却被判为未完成。**实测**：某习题文件真占位 7 处，简单 grep 却数出 9 处；改用上面的整行匹配后正确数出 7。
    （若想让 `sorry` 直接变成硬错误，可在文件顶部加 `set_option warningAsError true`——**实测**：含 `sorry` → `error` 且 exit 1；真证明 → exit 0。）
16. **`;` 与 `·` 不可混用**：`t1; t2` 会把 `t2` 作用到 `t1` 产生的**所有**子目标，与 `·` 的聚焦语义冲突。写多目标证明时用**换行 + `·`**，**不要**写成 `apply h; · ...; · ...`。
    本项目布置习题时，AI 自写的参考解**正是这样静默出错的**（报 `No goals to be solved` 且留下 `unsolved goals`）——**所以参考解必须真的编译一遍，不能凭记忆断定有解**。
17. **`/-- ... -/` 是「文档注释」，必须紧贴一个声明**：单独悬空使用会让 Lean 直接报错——先是 `unexpected token '/--'`，紧接着一串 `unexpected end of input` 的**连锁报错，很难一眼看出真正原因**。
    - 给 `def` / `theorem` 写说明 → 用 `/-- ... -/`（正确，会生成文档）
    - **纯讲解、后面没有声明**（如习题里的「题 4：…」）→ **必须用普通块注释 `/- ... -/`**
    - ⚠️ **本坑已复发**：写 `02-1-习题.lean`（题 4/5/6）时踩到，**紧接着写 `02-2-示例.lean` 又踩到一次**。
    - **最容易复发的具体场景**：写完一个 `/-! ## 小节标题 -/` 之后，**顺手用 `/--` 写这个小节的说明** —— 那个位置后面没有声明，必然报错。
    - **防呆做法**：**除非紧贴在 `def`/`theorem`/`structure`/`inductive` 正上方，否则一律用 `/- -/`**。宁可用错（顶多不生成文档），不可悬空（直接编译失败）。
18. **习题里的 `sorry` 要单独占一行**：验收脚本靠「整行就是 `sorry`」检出未完成的题，所以
    ```lean
    def x : Nat :=
      sorry
    ```
    这种换行写法是**故意的**。不要「顺手」改成 `def x : Nat := sorry`——那样脚本会**漏检**，文件看起来完成了、实际上没做。
19. **讲义 / 示例最容易犯的错不是「讲错」，而是「讲得含混」或「用了没讲过的语法」**（2026-09-19，02-2 一课内被用户**连卡三次**）：
    - 用「同上」做**就近指代**，而它字面指向的其实是**另一行**（本意是 `.1`，字面却指向 `.2`）；
    - 写「两者意思一样」却**没写明哪一点一样**，而且那句话本身还是错的（`Nat.succ` 吃 1 个参数，`Nat.add` 吃 2 个）；
    - 在示例里用了 `example : a = b := rfl`，而 `example` / `=` / `rfl` **全是第 3 章的内容**，本课一个字都没讲。
    **对策见 D14 的三条硬规矩。** 写完逐行自检：**这一行用到的每个符号，我在哪一课讲过？**
20. **习题文件的「验证区」必须留成注释**（2026-09-19，写 02-3 时定的做法）：验证区（`#check` / `#eval` 那些用于核对预测题的语句）若处于**活动状态**，那么只要一跑 `bash TPIL/check.sh`，**答案就直接印在屏幕上**——预测题当场作废。**做法**：验证区整段用 `-- ` 注释掉，并在文件里写明「**先写答案，再取消注释核对**」。（`02-2-习题.lean` 是在这条之前写的，保持原样不动；`02-3-习题.lean` 起按本条执行。）
    ⚠️ **但这只解决了一半**：脚本不印答案，**注释文字本身却也可能是答案**（`-- 16`、`-- quad : Nat` 之类）——见 **D15**（2026-09-19 用户指出；**从 02-7 起**执行）。
21. **讲义自身的代码块也要实跑；「裸表达式」块必须先包装**（2026-09-19 起每课都做）：光有 `NN-示例.lean` 通过还不够——**讲义里 ```lean 代码块也必须抽出来编译**，否则注释里写的输出可能过时或写错。做法：抽出所有 ```lean 块 → 拼接 → `lake env lean`。
    ⚠️ **坑**：像 02-4 讲义 §2 那种只展示语法、不带 `#check` 的**裸表达式块**（单独一行 `fun (x : Nat) => x + 5`），在命令层是**语法错误**，会让整次检查**假失败**（不是讲义内容有错）。
    **对策（已实测）**：拼接时**按行**处理——① 行首带缩进的行视为**续行**，照抄；② 已是命令的（`#` / `def` / `/-` / `--` / `example` …）照抄；③ 其余**裸表达式行前面补 `#check `**。02-4 讲义 16 段代码块经此处理全部通过（零 error、零 warning）。
    - ⚠️ **第二个坑（02-5 踩到）：讲义里「故意会报错」的代码块，不能用 ```lean 标记。** 否则抽块实跑会把它当成应当成功的例子，**整次检查假失败**。**做法**：那种块改用**普通代码块**（不带 `lean` 标签），并在正文写明「**下面这段是故意写错的**」。02-5 讲义有两处（`def bad : Nat := true`、`def five : Natural := 5`）。
    - ✅ **这个检查确实有产出**：写 02-5 讲义时它抓到一处**助手自己的不一致**——正文声称「`#eval` 全都得 `6`」，而对应的代码块里**只有 `#check`、根本没有 `#eval`**（读者看不到、也验不了）。补上后 19 行输出与注释逐字一致。**结论：凡是声称的输出，都必须真的出现在可跑代码块里。**
22. **Lean 的块注释是 `/- … -/`，不是 `/* … */`**（2026-09-19，写 `02-6-习题.lean` 时踩到）：把题面注释的结尾误写成 `*/`，Lean 报的是 **`unterminated comment`，且位置指向文件末尾**；还会**连带出一条与真正原因毫无关系的错**（`elaboration function for 'sorry' has not been implemented`），**极易误导**。
    - **原因**：`*/` 不是 Lean 的注释结束符，那个 `/-` 于是**一直没关上**（Lean 的块注释**可嵌套**；另一类注释坑见 §6.17 的 `/--` 悬空）。一条 `/-` 没关，后面整段代码都被当成注释，报错自然冒在别处。
    - **对策**：`.lean` 文件写完**先跑一遍** `lake env lean`；看到**文件末尾**的 `unterminated comment` 时，**回头找哪一处 `/-` 缺配对的 `-/`**，不要盯着末尾那行看。

---

## 7. 待形成的约定（占位，请勿凭空填充）

以下内容**尚未确定**，待实际实践中形成后再回写；形成前请勿在代码或文档里当作既定规则：

- [x] ~~主线形态与目录组织~~ → **已定为跟练 TPIL，自第 1 章起、一节不跳**（2026-09-17 修订），目录约定见 D9
- [x] ~~Lean 工具链版本~~ → **已固定 `leanprover/lean4:v4.34.0`**，见 D7（mathlib 暂不引入，需要时再定）
- [ ] 命名约定：文件/模块/定理命名风格（`snake_case`、`UpperCamelCase`、命名空间划分）
- [ ] 证明风格：是否偏好 `calc`/`linarith`/`simp` 等 tactic 的取舍，及 `sorry` 的使用边界
- [ ] 是否保留 `#check`/`#eval` 等调试痕迹；注释与文档字符串规范
- [ ] 提交粒度与提交信息格式（当前约定见 §8）
- [ ] 是否创建 `CLAUDE.md -> AGENTS.md` 符号链接（参考实现仓库的做法）
- [x] ~~是否补充 `README.md`~~ → **已完成**，见 D5
- [x] ~~是否选定 `LICENSE`~~ → **已定为 MIT**，见 D6

---

## 8. 工作方式约定

- **动手前**：阅读本文件；若涉及 `§7` 未定项，先问用户，不要自行拍板成「约定」。
- **改动后**：能验证的必须验证（编译/运行/测试），并在回复中说明验证方式与结果；**不要声称已验证而实际未验证**。
- **不确定就说不确定**：区分「已实测事实」「推测」「待确认」，不要把推测写成事实。
- **不擅自扩大范围**：用户说只做 A，就不要顺手改 B；发现别的问题，提出来，不擅自改。
- **失败要暴露**：命令失败要报告退出码与关键输出，不要掩盖或绕过。
- **git 提交与推送 ⚠️ 必须由用户主动发起**（见 D8）：**AI 不得自行 `git commit` 或 `git push`**——即便只是文档同步或本文件的维护也不例外。改完后应**列出待提交内容**并等待用户指示。
- **提交信息**：中文，首行简短概括，必要时正文说明「为什么」；一次提交聚焦一件事。
- **推送**：走 SSH 远程 `origin`。推送前先 `git status` 确认没有误提交构建产物或本机路径。
- **远程操作**：`gh` 已登录且 `git_protocol=ssh`。创建仓库用 `gh repo create`（SSH 本身不能创建仓库，见 §6.11）；查询状态优先用 `gh` 而非匿名 `curl`（见 §6.10）。

---

## 9. 本文件的维护协议（AI 助手职责）

**由 AI 助手主动维护**，用户不必提醒。满足以下任一触发条件即更新本文件：

1. 形成了新的**重复性工作流**或稳定做法（→ 写进对应章节）。
2. 用户**纠正**了助手的做法或偏好（→ 立即固化，避免重犯）。
3. **踩到新坑并解决**（→ 追加到 §6，附对策）。
4. **环境或工具链发生变化**（→ 更新 §2）。
5. 做出了**影响后续的决策**（→ 追加到 §3）。
6. `§7` 中某项**从待定变为确定**（→ 移出占位，写成正式约定，勾掉条目）。
7. 发现本文件**内容已过时或与事实不符**（→ 修正，而不是绕过）。

**维护规则**：

- 每次更新同步在 §10 追加一行变更记录。
- **§10 的粒度**：**新条目照旧详细**；**旧条目可压缩为一行摘要**（详情看 git 历史）——本文件受 **65536 字节的「工作区指令」上限**约束，**超了会被从尾部截断**（最坏情况是切掉最新条目）。压缩时按「**越旧越简**」处理。
- **维护 ≠ 提交**：更新本文件只落到**工作区**；**是否提交、何时提交一律由用户决定**（见 D8）。每次更新后必须主动告知用户「当前有哪些待提交的改动」。
- 保持「已验证事实 / 推测 / 待确认」的区分，**不写入未经确认的推测**。
- 优先**增量修订**，保持结构稳定；大改需向用户说明理由。
- 本文件应保持可读、可执行：多用具体命令、路径、对策，少用空泛表述。

---

## 10. 变更记录

> **格式约定（2026-09-19 起）**：**旧条目只保留一行摘要**，详细内容见 git 历史（`git log -p -- AGENTS.md`）。
> 原因：本文件是「工作区指令」，有 **65536 字节上限**；2026-09-19 实测涨到 66 KB 后被**从尾部截断**，
> 恰好切掉最新条目（最坏情况）。**新条目照旧详细记录**；再次逼近上限时，先压缩更旧的条目
> （或改用独立 `CHANGELOG.md`，见 B 方案）。

| 日期 | 版本 | 变更 | 说明 |
|---|---|---|---|
| 2026-09-17 | v0.1–v0.6 | 建档、建仓库、README、徽章、MIT | 固化环境事实（§2）、关键决策（§3）、通用约定（§5）、踩坑清单（§6）与维护协议（§9）；建 git 仓库并托管 LF（D4）；推送 GitHub 并记录认证事实（含 `gh` token 位置）；创建 `README.md` 并声明「AI 主导」（D5）；修复其中文徽章（§6.12 须百分号编码）；确定 MIT 许可证（D6） |
| 2026-09-17 | v0.7–v0.8 | 最小可编译基线；代理用法实测 | 装 elan `4.2.4` + Lean `v4.34.0`，建 lake 项目；`.lake` 符号链接**实测成立**（D1 由计划变已验）；`lake build` 25 秒通过并做反向测试；新增 §6.13（Lake 根模块）、§6.14（网络不稳需重试）；用户给出 WSL 代理方案（`ip route show default` 取网关 + 端口 10808），实测写入 §6.14 |
| 2026-09-17 | v0.9 | 仓库改 PUBLIC，加公开约束 | 用户确认属有意为之；D4 新增「**严禁写入凭据 / 私钥 / 内网地址**」；§6.10 改写为「可见性会改变 API 行为（200 vs 404），判断要用 `gh` 而非匿名请求」 |
| 2026-09-17 | v0.10 | **新增 D8：git 提交必须由用户主动发起** | 硬约束——AI 不得自行 `commit` / `push`，文档同步与 `AGENTS.md` 维护也不例外 |
| 2026-09-17 | v0.11–v0.11.1 | **正式开课：新增 D9**；修正验收命令 | 用户选「人类写证明、AI 批改」+ TPIL，建立 `TPIL/` 并布置第 1 批习题；**项目性质改为「AI 主导、人类习作」**。新增 §6.15（`sorry` 只是 warning、退出码 0；验收须整行匹配——原先用 `grep -n 'sorry'` 会误伤注释，真占位 7 处却数出 9 处）、§6.16（`;` 与 `·` 混用会让参考解静默出错） |
| 2026-09-17 | v0.12–v0.13 | 两条代理路径补齐；**新增 D10** | §6.14 补「SSH 要走代理只能用 `ProxyCommand`」；D10 = 用户指定的「配 `http.proxy` / `https.proxy` → 提交 → **务必清代理**」流程，并实测其范围：对 HTTPS 远程有效、对 **SSH 远程完全无效**（本仓库 `origin` 是 SSH） |
| 2026-09-17 | v0.14 | **推翻起止点：改为从第 1 章起** | 用户反馈「从第 3 章开始完全看不懂」，查证确认是**助手判断失误**（卡住的概念全在 2.1 / 2.2）；第 2 章由 9 课拆为 **10 课**；新增 `TPIL/学习计划.md`、`01-1-讲义.md`；`03-*` 暂不启用。另新增 §6.17（`/--` 悬空会报错）、§6.18（习题 `sorry` 须独占一行） |
| 2026-09-17 | v0.15–v0.16 | **新增 D11：讲义必须自包含中文**（后扩大适用范围） | 用户两次强调读英文吃力 → 讲义**必须自包含**，不得把读懂原文设为前提；随后扩大到**一切产出**（回复、注释、提交信息、文档一律中文；唯一例外是 Lean 关键字且须随文解释）；加自检问句「一个只懂中文的人能看懂吗？」。另记录 `~/DSHSpace` 是 `/mnt/e/DSHSpace` 的符号链接 |
| 2026-09-17 | v0.17–v0.18 | 第 1 课满分；**新增 D12：`check.sh`** | 用户 6/6 全对 → 开第 2 课（`02-2-*` 三件套）；因用户手敲中文文件名会字符错位，新增纯 ASCII 参数的 `bash TPIL/check.sh <课号>`（示例要求零警告零错误；习题按整行 `sorry` 计数；**「还没做完」返回 0**）；§6.17 记该坑复发 |
| 2026-09-17 | v0.19–v0.20 | 清理无关项目信息；**新增 D13：子目录自治** | 用户指出 README 过时 + 本文件引用了无关项目（**公开仓库不该外泄**）→ 删除并把 §5 改写为「本项目自己的约定」，D4 新增「不得写入与本项目无关的内容」；D13 = **任何子目录都可能自带 `AGENTS.md`**，主文件**有意不列举**，子文件不得推翻 D2 / D4 / D8 |
| 2026-09-19 | v0.21 | **新增 D14（材料写作三条硬规矩）**；`02-2` 收尾并开第 3 课 | 用户在一课内连卡三次，均非「内容太难」而是材料写得含混或越界 → 立 D14：禁用就近指代、说「一样」须写明哪一点、材料不得出现本课未讲解的语法概念。另补 §6.14（`web_fetch` 不走代理，改用 `bash` + `curl` + 代理兜底）、§6.20（习题验证区必须留成注释）。开 `02-3-*`（`Prop` 与 `Type`），并把宇宙多态标为「本课不引入」 |
| 2026-09-19 | v0.21.1 | 从仓库中删除 WSL 网关的具体地址 | 用户指出：既然取 IP 的办法已经写明，具体地址就不该入库（必然过时）。已删除并写明「不要把具体地址写进仓库」（呼应 D4）。**遗留**：旧提交 `5c29367` 的历史里仍有该地址（属私有网段网关，非凭据），未做历史重写 |
| 2026-09-19 | v0.22 | 第 2 章第 3 课**满分通过**（7/7） | 首轮 6 题全对 + 题 5 第二问漏答，补答后满分；题 7（四项分类）每项都写出判据证据，说明 02-2 第 9 节的判据已掌握；§6.20 的「验证区留成注释」流程奏效 |
| 2026-09-19 | v0.23 | **开第 2 章第 4 课（`fun` 与函数抽象）** | 发出三件套（示例 23 行实测输出、零 warning；习题 8 题）；参考解已验证有解；讲义 16 段代码块全部实跑；发现并记录 **§6.21**（裸表达式代码块需先补 `#check ` 才能编译）；边界：按原文自述「需先懂 dependent products」，把「类型当参数」标为本课不引入；并处理了原书示例在 Lean 4.34 下触发 `unusedVariables` 警告的问题 |
| 2026-09-19 | v0.24 | 第 2 章第 4 课**满分通过**（8/8）；修 `check.sh` 警告计数；改讲义 `?m` 一处错话 | 首轮 5 题全对 + 3 处订正后满分（题 3 参数顺序——**题面歧义有一半是助手责任**，已补进讲义第 8 节；题 4 / 题 8① 猜错 `fun` 的打印形式）。用户追问 `?m.7` 来历 → 实测澄清编号是「待定」的创建序号、`?m.9 x` 的 `x` 表示可能依赖（归 2-9），并改正讲义第 4 节一句错话、新增一小节。`check.sh` 警告数原先会多算（把提示文字里的 warning 也数进去），已改为只数诊断行并加退出码双保险（回归 02-1 / 02-2 / 02-3 通过） |
| 2026-09-19 | v0.25 | **开第 2 章第 5 课（定义：`def`）** | 发出三件套（讲义 10 节 / 示例零 warning / 习题 8 题，验证区按 §6.20 留注释）；**参考解 8 题全部编译验证**；讲义 11 段代码块实跑；`if` 与「把类型当参数」明确标为**不引入**；核心是 **semireducible（半可归约）**。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.26 | 第 2 章第 5 课**满分通过**（8/8） | 首轮 6 题全对 + 题 5 / 题 6 订正后满分。两处同根：**把「不写类型」误当成「显示成箭头式」**（实为望远镜式）——⚠️ **已是第三次栽在「猜显示形状」上**，故在讲义 §2 补**速查表**。另记下助手一次失误：曾声称「已补进讲义」而**实际未写入文件**——**凡声称「已改」，必须当场核实文件** |
| 2026-09-19 | v0.27 | **压缩 §10 旧条目（方案 A，用户拍板）** | 本文件曾涨到 66,658 字节、**超出 65,536 上限而被截断**（恰好切掉最新条目），故按「越旧越简」把 v0.20 及以前压成摘要、v0.21–v0.24 各压成 1 行。**要精确值就 `wc -c AGENTS.md`**；再逼近上限就继续压缩更旧条目，或改用独立 `CHANGELOG.md`（B 方案） |
| 2026-09-19 | v0.28 | **开第 2 章第 6 课（局部定义：`let`）；修正本课范围** | 核对官网原文后确认 **2.5 只讲 `let`**（原课表写的 `let`/`have`/`show` 是助手旧猜测；`have` 属原文 3.4、`show` 与证明目标有关，**都归第 3 章**），故本课只讲 `let` 并明确标出「不引入」。三件套发出：讲义 10 节、示例零 warning、习题 8 题；**参考解 8 题全部编译验证**；**讲义 9 段代码块实跑**；新增 **§6.22**（`*/` 不是 Lean 注释结束符，误写会报**文件末尾**的 `unterminated comment`）。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.29 | **新增 D15（习题不得泄露答案）**；02-6 **首轮批改** | 用户在批改前指出：**习题的验证区注释里写了答案、题面也把答案写了出来**——**批评成立**：§6.20 只挡住了「一跑脚本就印答案」，**没挡住「答案就写在注释里」** → 立 **D15**（题面只给「要什么 + 约束 + 提示」；验证区注释不得写预期输出；出完题须**假装自己是学生**自检「能不能直接抄出答案」）。**用户明确说本次不回头改，故 `02-6-习题.lean` 保持原样，从 02-7 起执行**；§5 新增第 10 条、§6.20 补指针。**02-6 首轮批改：4 题全对（题 2 换行 `let`、题 3 给函数起名、题 4 串联 `=12`、题 6 `#check` 显示与「只查类型不计算」），4 题待订正**——① 题 1 写成 `let a = w * h`（**Lean 的 `let` 只认 `:=`**，实测报 `unexpected token '='; expected ':=' or '|'`，并**连带**让验证区 `#eval area 3 4` 报「depends on the 'sorry' axiom」，**根子在第 23 行**）；② 题 5② 答成「新起的那个 `y`」（应为**前一个** `y`：若是自引用，Lean 直接报未知标识符，算不出 `11`）；③ 题 7 只说了「结果都是 16」，**没点透**「`let` 的名字是**替换**、`fun` 的参数是**变量**」，且把名字叫成「临时变量」不准确；④ 题 8 前半（`foo` 是 `Nat` 的别名、`x : a` 就是 `x : Nat`）**答对**，后半把 `bar` 里的 `a` 说成「未定义」——**错**：`a` 是外层 `fun` 的**参数/变量**，失败是因为函数体必须对**任意** `a` 说得通。**流程上再次出现同一现象**：验证区已印出 `11`，答案栏的解释却没回头改（同 02-5）。§1、§4、`TPIL/进度.md` 同步 |
| 2026-09-19 | v0.30 | 第 2 章第 6 课**满分通过**（8/8） | 首轮 4 题全对 + **4 处订正后满分**（题 1 `=` → `:=`；题 5② 改为**前一个** `y`；题 7 改为「`let` 的名字是**别名/缩写**、`fun` 的参数是**变量**」；题 8 后半改为「`a` 是外层 `fun` 的**参数/变量**，失败是因为函数体必须对任意 `a` 说得通」）。**订正一次到位且只改这 4 处**；`check.sh` 显示 **✅ 验收通过**，实测输出与讲义/示例逐字一致。用户**先判断、后核对**的流程全程照做。§1、§4、`TPIL/进度.md` 同步；下一课 **2-7（`variable` / `section`）** |

# CHANGELOG — Lean4Practice

> 本文件是 [`AGENTS.md`](./AGENTS.md) 的**变更记录**（原 `AGENTS.md` §10）。
>
> **为什么单独一个文件（B 方案）**：`AGENTS.md` 是「工作区指令」，受 **65536 字节上限**约束——
> 实测涨到 ~66 KB 时会被**从尾部截断**（最坏情况恰好切掉最新条目）。而变更记录是全文体量最大的一段，
> 所以 **2026-09-19** 把它整段搬到这里；`AGENTS.md` §10 只留一个指针，`AGENTS.md` 腾出约 9 KB。
>
> **格式约定（2026-09-19 起）**：**新条目照旧详细**；**旧条目只保留一行摘要**（详情看 git 历史：
> `git log -p -- AGENTS.md`）。压缩时按「**越旧越简**」处理。
>
> **维护**：每次更新 `AGENTS.md`，**必须同时在本文件追加一行**（见 `AGENTS.md` §9）。

> **格式约定（2026-09-19 起）**：**旧条目只保留一行摘要**，详细内容见 git 历史（`git log -p -- AGENTS.md`）。
> 原因：本文件是「工作区指令」，有 **65536 字节上限**；2026-09-19 实测涨到 66 KB 后被**从尾部截断**，
> 恰好切掉最新条目（最坏情况）。**新条目照旧详细记录**；再次逼近上限时，先压缩更旧的条目
> （或改用独立 `CHANGELOG.md`，见 B 方案）。

| 日期 | 版本 | 变更 | 说明 |
|---|---|---|---|
| 2026-09-17 | v0.1–v0.6 | 建档、建仓库、README、徽章、MIT | 固化环境事实（§2）、关键决策（§3）、通用约定（§5）、踩坑清单（§6）与维护协议（§9）；建 git 仓库并托管 LF（D4）；推送 GitHub 并记录认证事实（含 `gh` token 位置）；创建 `README.md` 并声明「AI 主导」（D5）；修复其中文徽章（§6.12 须百分号编码）；确定 MIT 许可证（D6） |
| 2026-09-17 | v0.7–v0.8 | 最小可编译基线；代理用法实测 | 装 elan `4.2.4` + Lean `v4.34.0`，建 lake 项目；`.lake` 符号链接**实测成立**（D1 由计划变已验）；`lake build` 25 秒通过并做反向测试；新增 §6.13（Lake 根模块）、§6.14（网络不稳需重试）；用户给出 WSL 代理方案（`ip route show default` 取网关 + 端口 10808），实测写入 §6.14 |
| 2026-09-17 | v0.9 | 仓库改 PUBLIC，加公开约束 | 用户确认属有意为之；D4 新增「**严禁写入凭据 / 私钥 / 内网地址**」；§6.10 改写为「可见性会改变 API 行为（200 vs 404），判断要用 `gh` 而非匿名请求」 |
| 2026-09-17 | v0.10 | **新增 D8：git 提交必须由用户主动发起** | 硬约束——AI 不得自行 `commit` / `push`，文档同步与 `AGENTS.md` 维护也不例外 |
| 2026-09-17 | v0.11–v0.11.1 | **正式开课：新增 D9**；修正验收命令 | 用户选「人类写证明、AI 批改」+ TPIL，建立 `TPIL/` 并布置第 1 批习题；**项目性质改为「AI 主导、人类习作」**。新增 §6.15（`sorry` 只是 warning、退出码 0；验收须整行匹配——原先用 `grep -n 'sorry'` 会误伤注释，真占位 7 处却数出 9 处）、§6.16（`;` 与 `·` 混用会让参考解静默出错） |
| 2026-09-17 | v0.12–v0.13 | 两条代理路径补齐；**新增 D10** | §6.14 补「SSH 要走代理只能用 `ProxyCommand`」；D10 = 用户指定的「配 `http.proxy` / `https.proxy` → 提交 → **务必清代理**」流程，并实测其范围：对 HTTPS 远程有效、对 **SSH 远程完全无效**（本仓库 `origin` 是 SSH） |
| 2026-09-17 | v0.14 | **推翻起止点：改为从第 1 章起** | 用户反馈「从第 3 章开始完全看不懂」，查证确认是**助手判断失误**（卡住的概念全在 2.1 / 2.2）；第 2 章由 9 课拆为 **10 课**；新增 `TPIL/学习计划.md`、`01-1-讲义.md`；`03-*` 暂不启用。另新增 §6.17（`/--` 悬空会报错）、§6.18（习题 `sorry` 须独占一行） |
| 2026-09-17 | v0.15–v0.16 | **新增 D11：讲义必须自包含中文**（后扩大适用范围） | 用户两次强调读英文吃力 → 讲义**必须自包含**，不得把读懂原文设为前提；随后扩大到**一切产出**（回复、注释、提交信息、文档一律中文；唯一例外是 Lean 关键字且须随文解释）；加自检问句「一个只懂中文的人能看懂吗？」。另记录 `~/DSHSpace` 是 `/mnt/e/DSHSpace` 的符号链接 |
| 2026-09-17 | v0.17–v0.18 | 第 1 课满分；**新增 D12：`check.sh`** | 用户 6/6 全对 → 开第 2 课（`02-2-*` 三件套）；因用户手敲中文文件名会字符错位，新增纯 ASCII 参数的 `bash TPIL/check.sh <课号>`（示例要求零警告零错误；习题按整行 `sorry` 计数；**「还没做完」返回 0**）；§6.17 记该坑复发 |
| 2026-09-17 | v0.19–v0.20 | 清理无关项目信息；**新增 D13：子目录自治** | 用户指出 README 过时 + 本文件引用了无关项目（**公开仓库不该外泄**）→ 删除并把 §5 改写为「本项目自己的约定」，D4 新增「不得写入与本项目无关的内容」；D13 = **任何子目录都可能自带 `AGENTS.md`**，主文件**有意不列举**，子文件不得推翻 D2 / D4 / D8 |
| 2026-09-19 | v0.21–v0.24 | D14 立规；02-2 收尾、开 02-3 / 02-4；02-3、02-4 均满分 | 细节见 git 历史（`git log -p -- AGENTS.md`）。要点：新增 **D14**（材料不得含混、不得用未讲的语法）；补 §6.14（`web_fetch` 不走代理）、§6.20（验证区留成注释）、§6.21（裸表达式代码块须补 `#check `）；02-4 题 3 的题面歧义**有助手责任**；用户追问 `?m.7` 来历 → 实测澄清编号规律 |
| 2026-09-19 | v0.25–v0.27 | 02-5（`def`）开课并满分；§10 首次压缩 | 要点：02-5 三件套 + 参考解验证 + 讲义代码块实跑；核心 **semireducible（半可归约）**；用户两处错在「猜显示形状」→ 讲义 §2 补**速查表**；**助手曾声称「已改」而实际没写文件** → 立下「**凡声称已改，必须当场核实文件**」；§10 曾超限被截断，故压缩旧条目 |
| 2026-09-19 | v0.28–v0.30 | 02-6（`let`）开课并**满分通过**（8/8）；**新增 D15** | 核对原文后确认 **2.5 只讲 `let`**（`have` / `show` 归第 3 章）；用户指出**验证区注释里写了答案** → 立 **D15**（题面只给「要什么 + 约束 + 提示」）；**新增 §6.22**（`*/` 不是 Lean 注释结束符） |
| 2026-09-19 | v0.31–v0.34 | **开 02-7（`variable` / `section`）**；用户就 §5 追问三轮，讲义补讲 | 三件套 8 题（**D15 首次落地**）；**新增 §6.23**（`variable` 的参数加在最前面、类型标注写「右值类型」）。§5 三轮追问 → 补「组合形式」要拆讲（§6.24）、**「`fun` 来自「定义有参数」，与 `variable` 无关」**（§6.25 抽块检查掩盖跨块依赖 → 另加逐块单独编译；§6.26 讲现象要用反证钉死 + 请学习者复述并逐句判定措辞）；用户复述「`variable` 就是参数」基本正确 |
| 2026-09-19 | v0.35 | 02-7 **首轮批改**（7 题全对 + 题 4 待订正）；**发现出题骨架埋雷** | 题 1/2/3 定义全对、题 5–8 四道概念题全对。**题 4 两处偏差**：`#check doubleN` 实测是**望远镜式** `doubleN (n : Nat) : Nat`（不是 `doubleN : Nat -> Nat`）；`#print` 实测**必带类型**、且 `fun` 的参数**不带标注**（`def doubleN : Nat → Nat := fun n => n + n`）——⚠️ **第四次栽在「猜显示形状」**，但这次规则讲义里已有（02-5 §2 速查表 + 02-7 §4 对照表），属「**没去套用**」而非「没讲过」。**新增 §6.27**：骨架把要插入的 `variable`/`section` 放在 `/-- 题 N -/` 与 `def` 之间 → 学生一插就报 `unexpected token 'variable'`（**而变量其实照样生效**，答案对、文件红）；对策：题面说明改用普通注释 `/-`、或写明「插在注释**上面**」。另：验证区 114–116 行（`def fA`/`variable`/`def fB`）漏取消注释 → `Unknown identifier`。已按 §9 **压缩 §6.14**（代理那节只留规则与命令，省约 2.5 KB）。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.36 | 02-7 **满分通过**（8/8） | 订正一次到位：① 三处 `/--` 文档注释改为**普通注释 `/-`**（与「把 `variable` 挪到注释上面」**等价**，两种修法均实测）→ 3 个 error 消失；② 验证区 114–116 行取消注释 → `fA`/`fB` 可查；③ **题 4 改对**：`#check` → `doubleN (n : Nat) : Nat`（**望远镜式**）、`#print` → `def doubleN : Nat → Nat := fun n => n + n`。`bash TPIL/check.sh 02-7` → **✅ 验收通过**（零 error、零 `sorry`）。遗留（不影响判定）：验证区里自己加的注释中，`#check` 的形状仍写成箭头式。§1、§4、`进度.md` 同步；下一课 **2-8（`namespace`）** |
| 2026-09-19 | v0.37 | **开 02-8（`namespace` / `open`）** | 核对原文 2.7 后发出三件套（讲义 10 节 / 示例零 warning / 习题 8 题 = 3 写 + 5 判断）。**参考解 8 题全部验证**（`MyBox.five`、`Outer.Inner.seven`、`useInc = 6`、namespace 内 `#check a` 打印**全名** `Foo.a`）。**讲义 8 段 ```lean 块实跑（拼接零 error）、逐块 8/8 自包含**。**D15 落实**：题面只说「要什么」（「让它的全名是 `MyBox.five`」），不说怎么写。**吸取 §6.27 教训**：题面**一律用普通注释 `/- … -/`**，并在文件头写明原因。**当场抓到并修掉两个坑**：① §2 的「故意报错」块**误用了 ```lean 标记**——正是 §6.21 第二个坑，抽块检查当场报 `already been declared`；② 示例里 `open Box2` 带出的短名 `a`，把后面 `Box3.fa` 的 `#print` 显示挤成 `f Box3.a`（本该 `f a`）→ 把 `Box3` 改名 `p`/`g`/`gp` 解决。**新教训：同一文件里多个 namespace 的短名会互相抢占——示例里的名字要全局唯一。** §1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.38 | 02-8 **首轮批改**（7 题全对 + 题 7 理由写反） | **开课以来第一次「首轮批改时文件本来就干净」**（零 error 零 warning）——§6.27 的「题面改用普通注释」修法生效。题 1/2/3 全对（含**关的顺序**）、题 5/6/8 概念题全对；**题 4 是显示形状题（`#check` 打全名），一次就答对**——打破 02-4/02-5/02-7「四次栽在猜显示形状」的连败（02-5 §2 速查表 + 本课 §3 起作用）。**题 7 结论对、理由写反**：写成「先开的先关」，应是「**后开的先关**」，且与原结论自相矛盾。**新增 §6.28**：预测注释写完要**真的对一眼输出**（他验证区写的 `-- a : Nat` 与题 4 答案矛盾）。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.39 | **变更记录搬到 `CHANGELOG.md`（B 方案）**；02-8 **满分通过**（8/8） | `AGENTS.md` 一度涨到 64.7 KB（上限约 65.2 KB），每次更新都要压缩旧条目 → 现把原 §10 整段移到本文件，`AGENTS.md` §10 只留指针、§9 的维护规则同步改写，腾出约 9 KB。02-8 订正一次到位（题 7 理由改成「**先开的要后关**」、验证区注释改成 `Foo.a : Nat`）→ **8/8 全对**，`check.sh 02-8` ✅ 验收通过。§1、§4、`进度.md` 同步；下一课 **2-9（依赖类型）** |
| 2026-09-19 | v0.40 | **开 02-9（依赖类型）** | 核对原文 2.8 后发出三件套（讲义 9 节 / 示例零 warning / 习题 8 题 = 3 补定义 + 5 预测）。**范围**：**依赖函数类型** `(a : α) → β a`（并实测「`β` 不依赖 `a` 时 `#check ((a : Nat) → Nat)` 与 `#check (Nat → Nat)` **输出一模一样**」）+ **依赖对/sigma** `(a : α) × β a` / `Σ a : α, β a` / `⟨a,b⟩` / `Sigma.mk`，以及「取第二个分量，返回类型是 `β p.1`」。**参考解 3 题全部私有验证**（`zeroFin`、`packDep`、`getSnd`）。**讲义 8 段 ```lean 块实跑（拼接零 error）、逐块 8/8 自包含**。**明确不引入**：`@` 与 `{}`（原文说「马上解释」= 下一课 2-10）、**宇宙多态**（原文 sigma 例子用了 `universe u v` → 我改用不带 universe 的 `Type`，并说明「效果一样、适用范围窄一点」）、`Fin`/`Vector` 的 `inductive` 定义（第 7 章）。**实测记两笔**：① `#check depZero 3` 打的是 `Fin (3 + 1)` **不是** `Fin 4`（Lean 不替你算）；② `β p.1` 打印成 `β p.fst`（02-2 学过 `.1`/`.fst` 同一个东西）；③ 顺带发现 `#eval (3 : Fin 3)` **不报错而得 `0`**（`OfNat` 取模）——**容易误导，故未收进材料**。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.41 | 用户就 **02-9 题 1** 提问：「这题的回答是不是用到学过的知识之外的内容」——**问对了** | `0 : Fin (n+1)` 能成立，靠的是**类型类实例**（`OfNat`，**第 10 章**），而讲义只用「`Fin (n+1)` 里一定有 `0`」带过、**没标注有一条未学的规则在起作用**（违反 D14 第 3 条）。**实测证据**：`0 : Fin n`（一般 `n`）报 `failed to synthesize instance of type class OfNat (Fin n) 0`，**Lean 自己在报错里写「numerals are polymorphic in Lean」**；同一个 `0` 能待在 `Nat`/`Int`/`Fin 3`/`Fin (2+1)` 里，`#check (0)` 默认 `Nat`。已修：**讲义 §4 末尾补 ⚠️ 段落**（分两层：答案是 `0` 只需已学知识；「Lean 凭什么接受」才需要新机制）+ **§7 新增第 3 条**（数字多态/类型类，第 10 章，会用到不解释），示例与习题题 1 同步加注。**新增 §6.29**：凡「字面量落在非 `Nat` 类型上」或任何靠实例搜索才成立的写法，都要当场标注 + 给出不依赖该机制的理由；并记下「题的形状（`Fin (n+1)` 而非 `Fin n`）本身也要有理由、且最好讲给学生」。复核：讲义 **9 块**拼接零 error、逐块 **9/9** 自包含；check.sh 02-9 退出码 0 |
| 2026-09-19 | v0.42 | 02-9 题 1 **第二轮追问**：「我以为有一个通用的方式可以取最小的那个数」 | 用户的困惑 = 把「返回 `Fin (n+1)` 里最小的数」读成「**要写一个取最小元的通用操作**」（前几课答案都是构造出来的表达式）。**真相：依赖可以只在类型上，值完全不依赖参数**——实测 `#print depZero` → `fun n => 0`（**定义体里没有 `n`**）；`depZero 3 : Fin (3+1)` 而 `depZero 7 : Fin (7+1)`（类型变），`#eval` 都是 `0`（值不变）；对照 `#print secondOf` → `fun α β p => p.snd`（值**用到了** `p`）。**用户的直觉是对的**：通用取法确实存在，但**由 Lean 提供（数字多态）、不用学生写**。已修：**讲义 §5 末尾新增 💡 小节**（含 `#print` 实测 + `secondOf` 对照 + 「类型可依赖、值可以不依赖」一句）+ §4 加前向指针；示例新增同名 §5；习题题 1 加「别读成通用操作」的防误会提示。**⚠️ 同时自查出一次 D15 失误并改掉**：上一轮我在题 1 的提示里写了「`0` 能待在 `Fin (n+1)` 里」——**把答案写进了题面**。§6.29 追加两条（第二轮追问 + D15 自查失误）。复核：讲义 **10 块**（拼接零 error、逐块自包含情况见下）、示例零 warning |
| 2026-09-19 | v0.43 | 新增 **[`TPIL/Unicode速查.md`](./TPIL/Unicode速查.md)**（跨课参考）：非 ASCII 符号怎么打 | 用户（**美式键盘**）问「讲义里那些非 ASCII 符号各自应该如何输出？」——**真缺口**（材料从没系统讲过输入方式，学生只能复制粘贴）。先议「放 02-9 讲义附录」，用户改为**单独文件**（更合适：它跨课通用）。内容：**三条路**（纯 ASCII 写法 / VS Code `\` 缩写 / 复制粘贴）+ 实测 ASCII 替代表 + 按用途分组的 **178 条**缩写表 + 希腊字母表 + 成对括号技巧 + 官方出处。**数据取自官方** `vscode-lean4/lean4-unicode-input/src/abbreviations.json`（经**代理**取回 `HTTP 200`，共 1857 条；直连与 `web_fetch` 均失败——复现 §6.14 的结论），**生成脚本带断言自检**（每条缩写都与官方表核对过）。关键结论：**很多符号有 ASCII 写法**、**类型变量名随便叫**、`\` 缩写是**编辑器功能**不是语言的一部分、`!=` **不是** `≠`。**新增 §6.30**；02-2 / 02-9 讲义与 `进度.md` 加指针 |
| 2026-09-19 | v0.44 | `Unicode速查.md` **建成后当场得到实证** | 用户在 02-9 题 2 写了 `<a, b>`（用 **ASCII 尖括号**代替 `⟨` `⟩`）→ 编译错误 `unexpected token '<'; expected term`（实测复现）。**正解两条**（均已实测）：`⟨a, b⟩`（打 `\<` + 空格）或 **纯 ASCII 的 `Sigma.mk a b`**。已把这条写进 `Unicode速查.md` §2「另一个陷阱（最容易踩）」——**这正是建该文件的理由**。另记：§6.30 补一句「建成即被实证」 |
| 2026-09-19 | v0.45 | 02-9 **首轮批改**（5 题全对 + 3 处待订正） | 三道定义题全对（题 2 的 **尖括号 `<a,b>` → `⟨a,b⟩`** 已修好）；题 7、题 8 全对（**题 8 抓住了「给第一个参数取名字」的本质**）；`check.sh 02-9` → **✅ 验收通过**。**三处待订正**：① 题 4 后半 `#check zeroFin 3` 实测是 **`zeroFin 3 : Fin (3 + 1)`**（带实参、不化简）；② 题 5 **结论对理由错**——理由应是「**因为 `β` 不依赖 `a`**」（`α → β` = `(a : α) → β` 在「不依赖」时的记号），他写的「因为这是一个依赖函数的类型」**方向说反**；③ 题 6 `p.2` 的类型是 **`β p.1`**（打印 `β p.fst`）——**本课核心**。**验证区两处预测与实测不符**（`#check zeroFin 3`、`#check ((a : Nat) → Nat)` → `Nat → Nat : Type`）；**并且题 6 又出现「答案与自己的注释互相矛盾」**（注释里写对了 `β p.1`，答题写成 `β`）——**§6.28 第二次实证**。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.46 | 02-9 **二轮批改**（7 题全对 + 题 4 后半仍待订正） | 题 5 理由改对（「因为 `β` 不依赖 `a`」，有 `因为因为` 笔误）、**题 6 改对（`p.2 : β p.1`——本课核心拿下）**；其余题全对。**题 4 后半仍不对**：从 `Fin (n + 1)` 改成 **`Fin 4`**，两处错——① 少 `zeroFin 3 :` 前缀；② **`Fin 4` 是化简后的**，实测打印 **`Fin (3 + 1)`**（讲义 §4 用 ⚠️ 专门标过）。**⚠️ §6.28 第三次实证**：他**验证区里写的正是 `Fin (3+1)`**（方向对），**答题却写 `Fin 4`**——答案与自己的注释又不一致。另记：验证区把 `#check Fin` 注成 **`fun (n : Nat) : Type`**——实测是 **`Fin (n : Nat) : Type`**；**`#check` 给望远镜式、`fun … =>` 是 `#print` 的形状**（**02-5 §2 速查表**再次相关）。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.47 | 02-9 **通过（8/8，三轮订正）** | **验证区 11 条注释全部改对**（含把 `#check Fin` 从 `fun (n : Nat) : Type` 改成**望远镜式** `Fin (n : Nat) : Type`、`Typ` 笔误、「不化简」的 `Fin (3+1)`、前缀 `zeroFin 3 :`）。题 4 答案里**类型部分已对**，只差**输出格式的前缀 `zeroFin 3 :`**——**而他验证区里写的正是完整形式**（说明已懂）→ **判定通过，不再跑第四轮**，只请他把答案那一行补齐。**§6.28 累计第 4 次实证**（「答案与自己的验证区注释不一致」：02-8 题 4、02-9 首轮题 6、二轮题 4、三轮题 4）；已在 `进度.md` 里给学生**对策**：**把答案直接照抄验证区注释的那一行**。§1、§4、`进度.md` 同步；下一课 **2-10（隐式参数）** |
| 2026-09-19 | v0.48 | **开 02-10（隐式参数）——第 2 章最后一课** | 核对原文 2.9 后发出三件套（讲义 11 节 / 示例零 warning / 习题 8 题 = 3 改造定义 + 5 预测）。**范围**：`_`（让 Lean 填）+ `{α : Type}`（声明成隐式）+ `variable {α : Type}` + `(e : T)`（给类型帮推断）+ `@foo`（全变显式）+ **数字重载**（推不出时默认 `Nat`）+ 术语 **elaboration**。**三处刻意的「回头把话说圆」**：① **02-9 用户问的「数字是多态的」**→ 本课 §8 正名（并标注「实现机制=类型类，第 10 章」）；② **02-8 见过的 `@List.cons` 的 `@`** → 本课 §7；③ **02-7 的 `variable`** → 本课 §5。**明确不引入**：`universe u`/`Type u`/**`Sort`**（原文 2.9 例子与 `@id` 输出都带宇宙——我一律改用不带 universe 的 `Type`）、类型类、`{}` 的变体。**参考解 3 题全部私有验证**（`identEx` 改 `{α : Type}`、`variable` 改隐式、`@identEx Nat 7`）。**讲义 8 段 ```lean 块实跑（拼接零 error）、逐块 5/8 自包含**，其余 3 块（`Lst`、`myId` 的复用）**均已在正文注明依赖**——**其中 §7 那处是复核时才补上的**（初稿漏注，§6.25 要求）。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.49 | 02-10 **首轮批改**（4 题全对 + 4 题需订正） | **三道改造定义全对**（`{α : Type}` 写法、`variable` 改法、`@identEx Nat 7`）+ **题 7 全对**；`check.sh 02-10` → **✅ 验收通过**。**🐞 本轮核心误解（题 5 与题 8 是同一个）**：把**花括号**理解成「**把参数省略/隐藏掉**」——真相是**参数没被省略、它还在**（`#check identEx` → `identEx {α : Type} (x : α) : α`，**花括号就是标记**），只是**调用时默认不用写**。**另 3 处**：① 题 4 的 `?m.1` 理由只说了一半（漏「**没给它任何线索**」）、打印写成 `identEx (x : α) : α`；② 题 5 前半**说反**（`#check` 也会显示隐式参数；与 `@` 的真区别是**望远镜式 vs 箭头式**）；③ 题 6 理由用词不准（应是「**给类型→Lean 据此填上隐式参数**」，而非「显式化」）。**§6.28 第 5 次实证，且这次是系统性的**：验证区 **6 条注释与实测不符**（把一般形式 `α` 当成代入后的 `Nat`/`Bool`）。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.50 | 🎉 **02-10 通过（8/8）→ 第 2 章 10/10 全部收官** | 二轮**四处订正全部改对**：① 题 4 打印 `identEx : ?m.1 → ?m.1` + 理由补上「**没给它任何线索**」；② 题 5 用对了**望远镜式 / 箭头式**；③ 题 6 改成「**给类型 → Lean 据此填上隐式参数**」；④ 题 8 改成「`_` = **让 Lean 自己填的洞**」「花括号**调用时默认不用写**」——**两处核心误解都清掉了**。**✅ 验证区 12 条注释逐条与实测相符（12/12）**——**`§6.28` 那条毛病（答案/注释与实测不符）第 6 次出现后终于被消灭**（此前 5 次：02-8 题 4、02-9 首轮题 6 / 二轮题 4 / 三轮题 4、02-10 首轮 6 条）；**给他的对策「直接照抄 `lake env lean` 输出」生效**。唯一可补（不算错）：题 5 未写「`@` 之后参数**必须**写」。**第 2 章 10/10 均满分**（2-9 三轮、2-10 两轮订正，其余一次通过）。§1、§4、`进度.md`（含第 2 章总结表）同步；**下一站第 3 章「命题与证明」** |
| 2026-09-19 | v0.51 | **开第 3 章！3-1（命题即类型）材料已发** | 先拉下原文第 3 章全章结构（**7 节**，3.3 下还有 4 子节），按「一课只引入一个新概念」拆成 **10 课**（**路线图写入 `学习计划.md` §三·B**）：3-1 命题即类型 / 3-2 第一个证明 / 3-3 合取 / 3-4 析取 / 3-5 否定与假 / 3-6 逻辑等价 / 3-7 辅助子目标（`have`——**02-6 曾把 `have` 推迟到这里**，现在兑现）/ 3-8 经典逻辑 / 3-9 综合演练 / 3-10 综合习题。**3-1 的关键判断**：原文 3.1 **纯概念**（可写的证明在 3.2），所以 3-1 的动手部分放在**类型层级**（写 `Prop`），真正的「第一个证明」留给 3-2。**内容**：命题即类型（`Prop`/`Sort 0` 标注不引入）、蕴含=函数（**`→` 的双重身份**）、Curry–Howard、两种理解（构造主义 / 编码技巧 + 「有居民」）、**proof irrelevance**；实测含 `#check And/Or/Not/True/False`、`And p q` → `p ∧ q : Prop`、`Or (And p q) r` → `p ∧ q ∨ r : Prop`、**`p → q : Prop`**（Prop 对箭头封闭）、以及**同一个 `fun` 两种身份**（`fun x => x : Nat → Nat` / `fun hp => hp : p → p`）。**明确不引入**：`structure`（第 7 章）、`axiom`（危险）、`Sort`/`universe`/`Type u`。**⚠️ 新踩到一个坑（已记 §6.31）**：`fun hp : p => fun hq : q => hp` 会报 **unused variable warning**——**示例要求零 warning**，所以 03-2 起要么 `_hq`、要么 `fun _ =>`、要么 `set_option linter.unusedVariables false`（**原书 3.2 正是这么做的**）。**验证**：讲义 4 块拼接零 error、逐块 3/4 自包含（1 块已注明依赖）；参考解三题私有验证有解；示例零 warning、13 行输出与注释逐行相符；`check.sh 03-1` 退出码 0。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.52 | 03-1 **首轮批改**（6 题全对 + 2 处需订正） | **三道「动手题」全对**（在类型层级写命题：`p → (q → r)`、`Or (And p q) r`、`(And (p→q) (q→r)) → (p→r)`）+ 题 5/6/8 全对；`check.sh 03-1` ✅ 验收通过。**⚠️ 题 4 是我问糊了（责任在助手，新增 §6.32）**：我用了「打印出来的却是**别的东西**」这种**模糊指代**（D14 第 1 条禁止的类型），**学生直接回「你的第二问我看不懂！」**。正确解读：`#check And p q` → **`p ∧ q : Prop`**（Lean 有记号就用记号打印）。**题 7 措辞**改准（「按定义相等」而非「逻辑上等价」+ 补「不携带『为真』以外的信息」）。**验证区 4 条注释按实测改**（`#check And`→`And (a b : Prop) : Prop`、`#check And p q`→`p ∧ q : Prop` 等）——**§6.28 的新变体**：这次不是「凭记忆重打」，而是**「把自己写的当成输出」**；**对策不变：照抄 `lake env lean` 的结果**。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.53 | 03-1 **通过（8/8）** | 两处订正全部改对：题 4 改成 **`p ∧ q : Prop`** + 理由「**Lean 有记号时就用记号打印**」；题 7 补上「**按定义相等**」与「**不携带『命题为真』以外的信息**」。**验证区 12/12 逐条相符——连续两课满分**（02-10 二轮 + 本课），**§6.28 那条曾出现 6 次的老毛病看来真的改掉了**。**⚠️ 本轮助手自己的核对脚本先出错**：抽验证区时漏掉注释里的 `variable (p q r : Prop)`，后 4 条被误判 ❌（`Unknown identifier p`）；**补上后 12/12**——**教训：核对脚本必须完整还原上下文**（§6.25 的精神）。**一处可选小改进（不算错）**：题 7 末尾「证明的具体内容不影响命题的真值」没说到点上（真值只由命题决定，平凡；proof irrelevance 的要点是**证明之间**按定义相等）——建议删或改写。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.54 | **开 03-2（第一个证明）** | 三件套（讲义 13 节 / 示例零 warning / 习题 8 题 = **3 题写证明** + 5 预测）。**⭐ 本课是学生第一次真正写证明**，核心是「**证明 = 造出那个命题类型的项；`fun` 就是造法**」。内容：`theorem` 就是 `def`；`fun` 引入假设；**同一件事的几种写法**；`#print`（类型写成 `∀ {p q : Prop}, …`）；`show … from …`（只是标注，内部变 `have`）；参数左置；**把定理当函数用**；泛化复用到不同命题对；复合（推理传递性）。**⚠️ 本课第一次引入 `unused variable` warning 的正面对待**（用不上的参数加 `_`；原文用 `set_option linter.unusedVariables false`）。**明确不引入**：`axiom`（危险，改用「把假设写成参数」）、`∀` 完整讲解（第 4 章）、`have`（3-7）。**⚠️ 本轮验证抓到 3 个真问题（都在我自己的讲义里）**：① 讲义块里 `theorem` **没声明 `variable {p q : Prop}`** → `type of theorem is not a proposition`（**未声明变量被 autoBind 成 `Sort u`**）；② **同一个 `t1` 在两个块里重复声明** → 拼接报 `already been declared`；③ **一个代码块被写进了 `>` 引用块** → 抽块时把 `> ` 当成代码 → `unexpected token '>'`。**三处全已修**，并新增 **§6.33 / §6.34**；另把 §6/§7/§8 的「声称输出」改成**可跑块产出**（§6.21），顺手**改掉一处笔误**（`#print t1b` → `t1e`）。**新发现（已写进讲义 §5 第 5 点）**：**`theorem` 的语句必须是 `Prop`，`def` 不要求**——`theorem bad : p' → q' → p'`（未声明）报错，而同样的 `def good` ✅ 通过。**复核**：讲义 **8 块**拼接实跑零 error 且输出与声称逐字相符；逐块 **5/8 自包含**（3 块已注明依赖）；`check.sh 03-2` 退出码 0 |
| 2026-09-19 | v0.55 | ⚠️ **`AGENTS.md` 触发 65536 字节上限被截断 → 压缩 §6 旧条目** | 写 03-2 时 `AGENTS.md` 涨到 **66,224 字节**，**超过「工作区指令」上限** → 加载时被**从尾部截断**（§7–§10 被切掉，正好命中 §9/§10 里预警的风险；**磁盘文件本身完好**）。**处置**：按 §9 的「**旧条目可压缩为一行摘要**」，把 **§6.1–13、19、23、24、26**（早期条目，多与 §2 / D1 / D2 / D4 **重复**）压缩成一行——**条目编号全部保留**（`§6.x` 被 CHANGELOG 与正文大量引用）。**66,224 → 62,487 字节（省 3737），余量 3,049**。**教训**：§6 也会涨；**每次更新后要看一眼字节数**（`wc -c AGENTS.md`），别等被截断。**下次接近上限时，优先压缩 §6 里最旧、最重复的条目。** |
| 2026-09-19 | v0.56 | 03-2 **首轮批改**（6 题全对 + 2 处需订正） | **三道证明里题 1、题 2 对**（题 2 用 `_hp` 规避了 unused warning）；**题 3 错**：他把语句读成了 `(p → q → r) → p → q → r`（以为括号内外同序），于是把「**引入假设的顺序**」与「**喂给 `hf` 的顺序**」混为一谈 → 实测报 `Type mismatch`（`fun hq => hf hp hq` 是 `q → r`，却期望 `p → r`）。**正解**：引入顺序照抄语句箭头（`hf` → `q` → `p`），而喂给 `hf` 的是 `hp` 在前、`hq` 在后——**两个顺序是两件事**，这正是本题的考点；他随后确认**误读版下原写法确实正确**（已实测）。题 4/5/6 全对（题 4 建议把判据说成「`def` 冒号后那个类型本身是不是命题」）；**题 7 只是复述原文**（给了追问：若 `theorem` 也把证明体用到的变量加成参数，对使用者有什么麻烦）；**题 8 答「隐藏变量」需订正**（应为 02-9 的**依赖函数类型** `(x : α) → β x`）。验证区 `t1` 预测两处不符：`∀ {p q : Prop}` 的花括号（隐式变量，02-10）与 `_` 被打印成 `x`（讲义 §6 第 2 点）——两条都是讲过的规则没套用。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.57 | 03-2 **通过（8/8，两轮订正）** | 两处订正全部改对：**题 7** 改成「否则定理的使用者会由于无关的条件而困恼！」（**答到了「对使用者」的后果**，不再是复读）；**题 8** 改成「`∀` 就是 02-9 学的**依赖函数类型** `(x : α) → β x` 的另一种写法……结果类型依赖前面的变量」（完全正确）。**题 4 判据也按批改拧准**（「`def` 冒号后面的那个类型本身是命题」）。**三道证明全对**——题 3 已用他自己订正的版本（`fun hf => fun hq : q => fun hp : p => hf hp hq`，`#print` → `fun p q r hf hq hp => hf hp hq`）。**验证区 `t1` 注释已按实测改对**（`∀ {p q : Prop}` + `fun {p q} hp x => hp`）。`bash TPIL/check.sh 03-2` → **✅ 验收通过**（退出码 0、零 error、零 `sorry`；1 条 warning 来自验证区）。**遗留一处（不影响判定）**：`:87` 按批改把**注释**里的 `y` 改成 `_y`，**代码那行没改**——于是注释与代码、与实测输出（`fun x y => x : Nat → Bool → Nat`）三者互相打脸，warning 也还在；已作为 **§6.28 的新变体**记入（「改了注释、没改代码」）。另 `:75`–`:77`、`:88` 写的是**源码形状**、Lean 打的是**摊平形状**（如 `fun p hp => hp`），属**渲染差异**、非错误。§1、§4、`进度.md` 同步；**下一课 3-3（合取 `∧`）** |
| 2026-09-19 | v0.58 | 旧第 3 章材料**下架删除**；同步三处失效引用 | 旧材料 `TPIL/03-讲义.md`、`03-示例.lean`、`03-习题.lean`（按旧方式写、当时跳过第 1/2 章导致「完全看不懂」）**已由新拆的 `03-1…` / `03-2…` 取代，故从工作区删除**（git 历史仍可查）。随之修掉三处「文件已不存在却仍被引用」的过期内容：**`AGENTS.md` §4** 目录清单末行（原写 `03-9-*.md / 03-*.lean 【暂不启用】` → 改为「已于 2026-09-19 下架删除」）、**`AGENTS.md` §6.15** 验收示例命令（`TPIL/03-习题.lean` → `TPIL/03-2-习题.lean`）、**`TPIL/进度.md`** 的「⚠️ 暂时不要读的文件」一节（改写为「旧的第 3 章材料已下架」并给出 `git show` / `git log --diff-filter=D` 的查档命令）。属于 §9 触发条件 7（**内容已过时**）|
| 2026-09-19 | v0.59 | **开 03-3（合取 `∧`）**；⚠️ **纠正路线图里的一处内容错误** | 先核对原文 **3.3.1「Conjunction」**（整页抓下来后只抽该节）再写三件套（讲义 13 节 / 示例零 warning / 习题 8 题 = **3 题写证明 + 5 题预测判断**）。**内容**：`And.intro`（引入规则）、`And.left`/`And.right`（消去规则）、匿名构造子 `⟨,⟩`、点记号 `h.left`/`h.right` 与 `.1`/`.2`、**`example` 命令**（不命名、不入库）、嵌套**扁平化**（`⟨a,b,c⟩`，**只对右嵌套成立**——左嵌套实测报错）、**`∧` 与 `×` 的相似与区别**（`And : Prop→Prop→Prop` vs `Prod : Type u→Type v→…`，两边混用均实测报错）、优先级（`∧` 比 `→` 紧）与右结合。**⚠️ 纠正**：路线图原写 3-3 含 `cases`/`constructor`、3-4 含 `cases`/`apply`——**核对原文后确认 3.3.1/3.3.2 全部用证明项、一个 tactic 都没有**（tactic 正式讲解在第 5 章），已改掉这两行，并在讲义 §11 明确「本课不引入 tactic」。**⚠️ 本课实测出一个必踩坑并写进讲义 §8**：`⟨hp, hq⟩` 在「上下文推不出类型」时报 `Invalid ⟨...⟩ notation: The expected type of this term could not be determined`——**我自己在示例里也犯了一次**（`#check (fun (h : p ∧ q) => ⟨h.right, h.left⟩)`），被验证当场抓到、已加类型标注修好。**D9 参考解**：题 1-3 三份参考解**私下编译验证通过**（`ex1` 结合律方向、`ex2` 函数作用到一对、`ex3` 分量顺序反着来），未落入仓库。**验证**：讲义抽块 **17 块拼接零 error**、**逐块 17/17 自包含**；示例 **零 warning**；`check.sh 03-3` → 示例 ✅ 干净、习题 ⏳ 还有 3 处 `sorry`（正常待作答）；验证区（去掉题 7 两行故意报错的）取消注释后实测 exit 0。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.60 | **新增 §6.35：讲解一个对象时，例子只能用那个对象本身**（用户指出） | 用户问「我对 `theorem` 的理解准不准」（他的说法：`:` 与 `:=` 之间是「有待证明的命题」、`:=` 后是证明、**能编译通过的表达式就是可行证明**）。我在答复里却拿 **`def MyNat := Nat`**、**`#check (p → p)`**、**`def f : (p → p) → Nat`** 当主证据，末尾还预告 **`↔`**（属 3-6）→ **用户直接指出：「我们讨论的对象是 `theorem`，我看见你刚才的回答说很多 `theorem` 无关的事」**。**问题**：既是 §8「不擅自扩大范围」，也是 **D14 第 3 条**的变体（顺手引入未讲解的概念），而且越界内容还承担了主证据。**对策（§6.35）**：先锁定讨论对象、所有例子只用该对象；需要对照时**先声明「这里是为了对照才引入 `def`」**。**自检问句：我举的例子，本身是不是我们正在讨论的那个对象？** **同时记下这次讨论的实测产出**（都只用 `theorem`）：① `theorem e (p : Prop) : p → p := sorry` **编译通过、退出码 0**，但 `sorry` **不是证明** → 用户的判据「能编译通过 ⇒ 可行证明」**不充分**；② 文件里另有一条错的 `theorem` → 全文件 `exit 1`，但正确那条 `theorem` 的证明依然成立 → 该判据**也不必要**；③ **`theorem t2 … := t1` 实测报错**（`t1` 的完整类型是 `∀ (p q : Prop), p → q → p`，**参数也在类型里**），必须写 **`:= t1 p q`** —— 这正是 3-2 §8「把定理当函数用」；④ `theorem swapA … : And p q → And q p` 用 `And` 写、`#check` 打印成 `p ∧ q → q ∧ p`，而 `swapA p q` 能直接当 `p ∧ q → q ∧ p` 的证明 → **判据是「类型按定义相等」，与写法/打印无关** |
| 2026-09-19 | v0.61 | 03-3 **首轮批改**（8 题内容全对 + 验证区 7 条待订正） | **三道证明一次写对，且与 AI 私下的参考解逐字一致**（`#print` 实测：`fun p q r hl => ⟨⟨hl.left, hl.right.left⟩, hl.right.right⟩`、`fun p q r h1 h2 => ⟨h1 h2.left, h2.right⟩`、`fun p q hp hq => ⟨hq, hp⟩`）——题 3 **没有重蹈 03-2 题 3 的覆辙**。**五道判断题全对**：题 4 的 `#check And.intro` **逐字相符**（隐式参数的花括号 + 两个参数名 `left`/`right` 全猜中）；题 5 四个类型 `p , q , p , q` 全对；题 6 两问全对；题 7 两个结论 + 理由全对；题 8 两点全对。`check.sh 03-3` → **退出码 0、零 warning、零 error、无 `sorry`**。**⚠️ 验证区 7 条与实测不符（§6.28 第 7 次实证）**，分两类：① **参数名猜错**（`:94` `:95` 写 `(h : a ∧ b)`，实测是 `(self : a ∧ b)`）；② **把自己写的当成输出**（`:101`–`:104` 写 `fun (h : p ∧ q) : p`，实测是 `fun h => h.left : p ∧ q → p`——而且 **`h.1` 被打成 `h.left`、`h.2` 被打成 `h.right`**；`:112` 写 `demo : p → p`，实测 `demo (p : Prop) : p → p`）。**对上的 3 条**：`:92` `:99` `:100`。⚠️ **正确答案就在讲义 §5 的注释里**（那几行注释就是实测输出）——没去对照。**两处措辞可再精确**：题 7「虽然 Prop 也是一种类型」——现象对，但理由要落在对象上（`p` 是**命题**=`Prop` 的**元素**，而 `Prod` 的参数要 `Type u` 里的**类型**；`Prop` 自己虽 `: Type` 但**不是 `Type u`**，报错信息 Lean 自己就是这么写的）；题 8「example **通常**不被引用」——**措辞偏弱**，是**没有名字所以不能**被引用。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.62 | 🔵 **撤回首轮对 `:94`/`:95` 的判定**：讲义漏讲 `self`（材料缺口，已补） | 学生反问：「`self` 前面已有的讲义中**从来没有提到过**吧？什么意思？」——**问对了**。**事实核对**（grep）：`self` 在 `03-3-讲义.md` §5（:141/:143）与 `03-3-示例.lean`（:40/:42）**都出现过**，但**只是作为 `#check` 输出里的一串字符，从未解释**；而题 4 要求「把整行抄下来」= **逼他去猜一个没讲过的词**。→ **判定不公，已撤回**（「参数名猜错」改为「🔵 我的材料缺口」）。**实测真相**（`#print And.left` → `fun a b self => self.1`）：`self` **不是关键字、不是语法**，只是 Lean 库给「被取分量的那个东西」起的**参数名**（同 Python / Rust 方法里的 `self`）；**名字完全不起作用**——实测 `And.left h` / `And.left x` / `And.left self` **三种叫法都通过**，正体现「判据是类型，不是写法与名字」。**已修讲义 §5**：补 `#print And.left` 实测 + 三种叫法对照 + `#check @And.left` 的**箭头式**（无名字），并说明「为什么打印成 `(self : a ∧ b)`」是 **02-5 / 02-10 的望远镜式规律**。**复核**：讲义块数 17 → **20，拼接零 error、逐块 20/20 自包含**。**另 5 条**（`:101`–`:104`、`:112`，「把自己写的当成输出」）**仍成立**。⚠️ **`AGENTS.md` 余量只剩 1211 字节**——本轮**故意不往 §6 加新条目**（会超限），教训先记在 `CHANGELOG.md` 与讲义里；**开下一课（3-4）前必须先压缩 §6 旧条目** |
| 2026-09-19 | v0.63 | **03-3 二轮通过（8/8）；开 03-4（析取 `∨`）**；压缩 §6.29 | **03-3 二轮**：他自查后把 5 条验证区注释（`:101`–`:104`、`:112`）全部改成与实测**逐字一致**（含 `h.1` 被打成 `h.left`、`.2` 打成 `.right`、`demo (p : Prop)` 不漏参数），`:94`/`:95` 也主动改成 `self`；`check.sh 03-3` → **退出码 0、零 warning 零 error**，**8/8 通过**。**开 03-4**：讲义 11 节（`Or.inl`/`Or.inr`、全名 `Or.intro_left`/`Or.intro_right`、`Or.elim` 三参数与点记号 `h.elim`、**为什么 `⟨,⟩` 不能用于 `∨`（报错原文 `has more than one constructor`）**、`#print Or` 两个构造子 vs `#print And` 一个、`#print Or.elim` 里的 `match` 只讲意思）；示例零 warning；习题 8 题（3 证明 + 5 判断）。**⚠️ 出题时抓到自己的错**：题 1 的参考解我先写成 `fun hp => Or.inr hp` → `Application type mismatch: hp has type p but is expected to have type q`，改正为 `Or.inl hp` 才通过——**§6.16「参考解必须真编译」第 N 次生效**，已记为 **§6.36**。**验证**：讲义 7 块拼接零 error、**逐块 7/7 自包含**；示例零 warning；`check.sh 03-4` → 习题 ⏳ 3 处 `sorry`、示例 ✅ 干净、退出码 0。（另：写习题时我又犯了「块注释提前闭合」的错——预测题的「我的答案：-/」必须与题面在**同一个** `/- … -/` 里，否则 `-/` 落在注释外报 `expected token`；`check.sh` 当场抓到。）**压缩 §6.29**（64325 → 63215 字节，给 §6.36 腾空间）。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.64 | 03-4 **首轮批改**（7/8 + 2 处订正） | **三道证明一次写对，与参考解逐字一致**——`#print` 实测 `fun p q hp => Or.inl hp` / `fun p q hp x => Or.inl hp` / `fun p q r hpq hpr hqr => Or.elim hpq hpr hqr`；**题 1 没有踩 `inl`/`inr` 拿反的坑**（§6.36 的预警生效），题 2 用 `(_ : q)` 规避 unused warning → **零 warning**。`check.sh 03-4` → 退出码 0、零 warning 零 error。题 4/5/7/8 全对（`#check Or.inl`、`Or.elim` 类型行**逐字相符**）。**⚠️ 题 6 需订正**：他答「`p ∨ q` 只需一个成立、`p ∧ q` 需要两个」——**理由层次不对**：题面与提示都指向**机制**（`#print` 对读、找决定性的不同），且**报错原文自己就写明了原因**（`has more than one constructor`）。**决定性事实是「构造子的个数」**——用两个对照实验证实：① `Nat × Bool`（02-2 的「对」）**只有 1 个构造子** `Prod.mk` → `⟨,⟩` 可用；② **`List Nat` 与合取/析取毫无关系**，却有 **2 个构造子**（`nil`/`cons`）→ `⟨l1, l2⟩` 照样报同一错。**⚠️ 验证区 `:101` 与实测不符**：`Or.intro_right` 的隐式/显式参数写反（正确是 `{b : Prop} (a : Prop)`，**答案就在讲义 §3 与示例里**）；其余 4 条逐字相符（`:103`/`:104` 是笔记非预测，我的比对脚本因 `#print` 多行输出而失配，已排除）。**教训（暂记 CHANGELOG，`AGENTS.md` 余量仅 1180 字节、开 3-5 前再压 §6）**：**「语义上说得通」不等于「机制上的必要条件」**——遇到「为什么 X 不行」要问的是**必要条件**，不是**事理**。§1、§4、`进度.md` 同步 |
| 2026-09-19 | v0.65 | 03-4 **二轮通过（8/8）**；开 03-5（否定 `¬`）；新增 §6.37 / §6.38 | **03-4 二轮**：题 6 改成决定性理由（构造子个数）；验证区 `:101`（`Or.intro_right {b : Prop} (a : Prop)`）、`:103`/`:104` 笔记一并订正，**5 条注释全部逐字相符**，`check.sh 03-4` 退出码 0 零 warning → **8/8**。**开 03-5**：讲义 12 节（`False` **0 个构造子**、**`¬p` 与 `p → False` 按定义相等**（两个方向都实测）、证 `¬p` 的套路 = 3-2 的 `fun` + 造矛盾、`False.elim` 爆炸原理、`absurd` 参数顺序「先证明后否定」、`True.intro` 顺带、四个连接词总表、`¬` 优先级高于 `→`）；示例零 warning；习题 8 题（3 证明 + 5 判断）。**⚠️ 出题时实测出一个 Lean elaboration 坑（已写进讲义 §6 + §6.37）**：把一个 `fun`（body 是「应用」）交给否定假设时，**目标末尾写 `False` 能过、写 `¬X` 报 `Application type mismatch … expected to have type p`**；给 `h` 标类型 / `show … from` / 标 `fun` 参数 / 整体加括号**四种都救不回来**（各实测）。**⚠️ 出题时又两次犯「块注释提前闭合」**（03-4、03-5），`我的答案： -/` 落在注释外报 `expected token`，`check.sh` 当场抓到 → 记为 **§6.38**。**验证**：讲义 11 块拼接零 error、**逐块 11/11 自包含**；示例零 warning；`check.sh 03-5` → 习题 ⏳ 3 处 `sorry`、示例 ✅、退出码 0；D9 三题参考解**先编译验证通过**（出题过程中被参考解编译抓到两处：① 多写没用到的 `q` 触发 unused warning；② `h.inl` **不是合法 field notation**（`Or.inl` 是构造子）；③ `¬p → ¬(p → q)` **根本不可证**，已换成可证且可写的题）。**⚠️ 差点撞上限**：加完 §6.37/38 后 `AGENTS.md` 达 **65351 字节**，工作区指令**从尾部被截断**（磁盘文件没坏，但载入时丢结尾）→ 追加压缩 §6.29/§6.31/§6.33，现 **64284 字节**、余 1252、§1–§10 与尾部齐全（**教训：每轮加条目前先看余量，别等被截**）。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.66 | 补讲义 03-5 §2：「造不出」≠「不存在」（学生提问引发） | 他把 §3 的证明体改成裸的 `False` 编译不通过，问「`False` 没有构造子，所以**必须有其他构造子**构造——对吗？」→ **方向对、机制说反了**。实测报错原文：`Type mismatch: False has type Prop of sort Type but is expected to have type False of sort Prop`——⚠️ **Lean 说的不是「找不到构造子」，而是类型不匹配**（`False` 是个**类型名**，其类型是 `Prop`；而那个位置要一份 **`False` 的证明**）。**决定性反证**：`example (h : False) : False := h` ✅（**手上已有一份，直接搬，一个构造子都不需要**）——所以规则不是「需要别的构造子」，而是「**不能从零造，但矛盾一旦存在那份证明就在**」。**已补讲义 §2**：三条准确说法（不能从零造 / 已有即可引用 / 矛盾通常由「把证明喂给否定假设」得到）+ **「造矛盾 vs 用矛盾」区分表**（`False.elim` 是**消耗**矛盾，不生产矛盾——这正好解释「`False` 只有消去规则没有引入规则」）。**复验**：讲义块数 11 → **12，拼接零 error、逐块 12/12 自包含**。留档进 `进度.md`；**另**：他的「动手实测两种写法」正是本项目最该有的习惯，值得肯定 |
| 2026-09-19 | v0.67 | 03-5 **首轮批改**（三道证明全对；题 5/7/8 需订正） | **三道证明一次写对**——`#print` 实测 `fun p h => absurd h.left fun hp => h.right hp` / `fun p q hnp hnq h => Or.elim h (fun hp => hnp hp) fun hq => hnq hq` / `fun p q hq h => h fun _hp => hq`；题 3 的 `h` 标注已从上轮的 `p → q` **改正为 `¬(p → q)`**。`check.sh 03-5` → 退出码 0、**零 warning**、无 `sorry`；**验证区 10 条逐字全对**（含 `#print` 多行块，**至今最干净**）。**⚠️ 题 5 概念错**：「`And` 有构造子 → `And` 是 Lean 里的『真』」——**有构造子只说明「怎么造」，不说明「成立」**：`p ∧ q` 造它需要 `p`、`q` 两份证明，而那两份未必存在；Lean 的「真」是 **`True`**。**⚠️ 题 7 依据弱**（与 03-4 题 6 **同类第 2 次**）：「都是函数」是语义层理由；「`¬p ≡ p → False`」是**定义陈述而非证据**——**证据是那两条能编译的 `example`（两个方向都过 = 按定义相等）**。**⚠️ 题 8 理由不成立**（结论对）：「第一个等价于 `q → p → q`」不准确（① 的类型是 `q → ((p → q) → False) → False`）；「第二个相反所以错」不是理由。**综合教训（第三次同型）**：**语义上说得通 / 引用定义 / 「相反」——都不能替代「可编译的机械证据」**。§1、`进度.md` 同步（本轮未新增 §6 条目，AGENTS.md 余量 1252 字节）|
| 2026-09-19 | v0.68 | 03-5 **二轮通过（8/8）**；开 03-6（逻辑等价 `↔`） | **03-5 二轮**：题 5 删掉「`And` 是『真』」的错（✅）；题 8 依据换成实测报错 `Application type mismatch`（✅）。**题 7 仍差一处**——他把「**它们代表的都是函数**」留作理由，且只引用 `example (h : ¬p) : p → False := h` **一个方向**；**一个方向证明不了「是同一个类型」，要两个方向都在**（或引用「按定义相等」）。**按 02-9 先例**（内容全对、只差一行）判通过、不另起一轮，这一行请他随时补。三道证明首轮即全对、验证区 10 条逐字全对。**开 03-6**：`Iff` / `↔`（原文 3.3.4）——**它不是新东西，而是 `(p → q) ∧ (q → p)` 的缩写**（与 `¬p` = `p → False` 同型），`Iff.intro` 两个方向、`Iff.mp` / `Iff.mpr` 取方向、**优先级最低且右结合**（`p → q ↔ r` 读作 `p → (q ↔ r)`）。§1、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.69 | 开 03-6（逻辑等价 `↔`） | 讲义 10 节 + 示例零 warning + 习题 8 题（3 证明 + 5 判断）。**实测结论**：`Iff` 是 **structure**、**只有 1 个构造子** `Iff.intro`（两个显式参数：**正向 `mp` 在前、反向 `mpr` 在后**）→ `⟨,⟩` 可用；字段 `h.mp`/`h.mpr`/`h.1`/`h.2` ✅，**`h.left` ❌**（`Iff` 没有 `left`）、`h.1.mp` ❌；⚠️ **`p ↔ q` 与 `(p → q) ∧ (q → p)` 不是同一个类型**（`: … := h` 报 Type mismatch）——**与 3-5 的 `¬p` = `p → False`（按定义相等）正好相反**（`Iff` 是 structure、`¬` 是 def）；⚠️ **优先级**：`→` 比 `↔` 结合得早，**`↔` 不能连写**（`p ↔ q ↔ r` 报 `unexpected token '↔'`）——**原文说「右结合」，实测不成立**。**D9 参考解编译抓到两处我自己的错**：① 备选题 `(p → q) ↔ (¬p → ¬p)` **在构造逻辑里根本不成立**（`¬q → ¬p` 加 `hq : q` 推不出 `p`）→ 换成 `(p ∨ q) ↔ (q ∨ p)`；② 讲义里 `and_swap` **重复声明三次**（拼接时 `already been declared`）→ 改名 `and_swap2`/`and_swap3`；另**故意写错的 `#check (p ↔ q ↔ r)` 一度被放进 ` ```lean ` 块**（§6.21 坑 2 第 3 次）→ 改普通代码块。**验证**：讲义 10 块拼接零 error、**逐块 10/10 自包含**；示例零 warning；`check.sh 03-6` → 习题 ⏳ 3 处 `sorry`、示例 ✅。§1、§4、`进度.md`、`学习计划.md` 同步 |
| 2026-09-19 | v0.70 | 03-5 **三轮：8/8 全课结束** | 题 7 最终改成「**是同一个东西。因为两个方向的 `example` 都能编译**」，并把两条都引上，弱理由「**都是函数**」删除——**这正是他自己建立的判据（类型按定义相等）在用**，也终结了 03-4 题 6 起的「语义理由 ≠ 机械判据」这条线（03-4 题 6 → 03-5 题 7 首轮 → 三轮改正，**共 3 次同型**）。`check.sh 03-5` → 退出码 0、**零 warning**、无 `sorry` → **8/8**。连同 **03-6 三件套**（讲义 10 节 / 示例零 warning / 习题 8 题）与全部文档同步一并提交 |
| 2026-09-19 | v0.71 | **修复 `进度.md` 被误截断的事故**（从 987 行截到 31 行，已在上一提交 push） | 用 `s[:i0] + 新块 + s[i1:]` 插入 03-6 当前位置块，而 `i1 = s.find('锚点', i0)` 在锚点已被上一轮改掉时返回 **−1** → `s[-1:]` 只剩最后一个字符 → **02-1～03-4 的全部学习记录被切掉**。**抽块检查与 `check.sh` 查不出这类损坏**（它们只管 `.lean`）——是提交统计里 `进度.md | 982 +------` 与 `rewrite (99%)` 暴露的。**修复**：用 `git show HEAD~1:TPIL/进度.md` 取回基线（987 行）→ 分步脚本（每步打印、`find` 结果逐一检查、先写临时文件校验再落盘）→ 重建为 **1027 行**，**22 条学习记录全在**（02-1～03-6），并补上 03-5 三轮条目、03-6 当前位置、进度表、03-5 学生提问条。**已记为 §6.39**（含 5 条对策：`find` 结果必须 `assert != -1`、绝不把可能为负的索引喂给切片、**改 `.md` 前后必须 `wc -l` 对比**、分步脚本先写临时文件、已 push 的损坏用 `git show HEAD~1` 取基线）。**另**：为腾余量压缩 §6.29 / §6.33，AGENTS.md 现 **65467 字节**（余 69，**偏紧，下轮加条目前要再压**） |
| 2026-09-19 | v0.72 | 03-6 **首轮批改**（三道证明全对；题 7 理由待补） | **三道证明一次写对**（`#print` 实测，与参考解**逐字等价**）：① `fun p => { mp := fun h => Or.elim h (fun hp => hp) fun hp => hp, mpr := fun hp => Or.inl hp }`；② 析取交换律**两个方向的 `inl`/`inr` 位置都换对了**（§6.36 那条坑没踩）；③ 对称性**方向没搞反**（`mp`↔`mpr` 对调正确）。`check.sh 03-6` → 退出码 0、**零 warning**、无 `sorry`。**验证区 12 条逐字全对**（两个多行 `#print` 块 7/7 行、8 条 `#check` 全对）。**题 4/5/6/8 全对**——题 6 尤其好：抓到「**这题答案与 3-5 题 7 相反**」的提示，依据用实测报错。**⚠️ 题 7 判断全对但理由全缺**（题目明确要求「逐个说明理由」）。**⚠️ 验证区 `:129`/`:130` 注释混了两种写法**：`Iff.mp : a → b` 是 `#print Iff` 里的**字段类型**，而 `#check` 打印的是 `Iff.mp {a b : Prop} (self : a ↔ b) : a → b`——**注释只放「这条命令的输出」**（§6.28 精神的新变体）。**自查工具教训**：我第一次比对验证区时又用「按行号对齐输出」的脚本，被多行 `#print` 带偏（**第 3 次**），改成**逐条隔离编译**才对；且隔离时要**带上 `variable` 声明**，否则 `p q r` 变 unknown identifier。§1、`进度.md` 同步（本次改文档全程遵守新立的 **§6.39**：只用 `replace`、`find` 必断言、**改 `.md` 前后 `wc -l` 对比**）|
| 2026-09-19 | v0.73 | 03-6 **二轮通过（8/8）**；开 03-7（辅助子目标 `have`/`suffices`） | **03-6 二轮**：题 7 补上四句理由（`h.mp`/`h.mpr` 因为 `#print Iff` 的 `fields:` 里有；`h.left` 因为 **`Iff` 没有 `left` 字段**；`h.1` = 第 1 个字段 = `mp`）；验证区 `:133`/`:134` 注释**只留命令输出** → `check.sh 03-6` 退出码 0 零 warning → **8/8**。**开 03-7**（原文 3.4）：讲义 9 节 + 示例零 warning + 习题 8 题。**实测结论**：① **`have h : p := s` 接 `t` 展开就是 `(fun (h : p) => t) s`**（原文原话，实测 `#print` 对照：`have` 版与手写 `fun`+应用版**是同一个证明**）；② **`let` 也能用在证明里**（02-6），差别只是读法（数据 vs 事实）——**不要记「`let` 在证明里不行」**；③ **`suffices` 内部也是 `have`**（`#print t2` 实测），差别是**行序反过来**（先写「有了它就够」，再证它）；④ `suffices` 漏写后半句是**语法错**（`unexpected end of input`），不是类型错；⑤ `have` 类型写错是**类型错**。**D9 参考解编译抓到两处我自己的错**：① `h.right.elim` 非法（`h : p ∧ q` 里 `h.right` 是 `q`、不是析取式）——**我又一次把 `∧` 看成 `∨`**；② 初版 ex3 命题用 `(p ∧ q)` 配两个蕴含假设，`suffices` 写出来是自我指派 → **换成 `(h : (p ∧ q) ∧ r) : p ∧ (q ∧ r)`**（真正体现「倒着想一次搞定」）。**抽块检查抓到 3 处**：① 「故意写错」的块又放进 ` ```lean `（**§6.21 坑 2 第 4 次**）；② `#print` 块**跨块依赖**（`t1`/`t2`/`t3` 在别的块，§6.25）→ 讲义里 `#print` 只贴**输出**（普通代码块），**可运行版留在示例文件**；③ 那个块还被**塞进列表**（缩进 3 空格，§6.34）→ 拉出来。**验证**：讲义 5 块拼接零 error、**逐块 5/5 自包含**；示例零 warning；`check.sh 03-7` → 习题 ⏳ 3 处 `sorry`、示例 ✅。为腾余量再压 §6.25/§6.28/§6.29/§6.32/§6.37/§6.39，AGENTS.md 现 65233 字节（余 303） |
| 2026-09-19 | v0.74 | 补讲 03-7 §3 的「`s` 和 `t` 到底是什么」；新增 §6.40 | 学生读讲义时问：「`have h : p := s` 接上 `t`」这句里，**以 `have hp : p := h.left` 这一行为例，`s` 和 `t` 分别是哪一部分？** —— **原讲义答不上来**：只给了全部展开后的 `t3`，没说 `t` 是「**这一行之后剩下的全部内容**」（多个 `have` 时**不是下一行**），也没说**后写的 `have` 在外面**。**实测给出「只展开第一行」的中间形态 `t1a`**（`(fun (hp : p) => have hq : q := h.right; show q ∧ p from And.intro hq hp) h.left`，**exit 0**，`#print` = `(fun hp => have hq := h.right; have this := ⟨hq, hp⟩; this) h.left`），据此修订讲义 §3（新增小节 + `t1a`；块数 5 → 6，**逐块 6/6 自包含、拼接零 error**）。**记为 §6.40**（「等价展开」必须补**逐步一一对应**；**编译通过 ≠ 读者能自己推出来**）。**顺带**：§6.21 坑 2 记为**第 4 次**复发；§6.14/§6.21/§6.22/§6.29/§6.35/§6.40 逐条按条目边界重写压缩，AGENTS.md 现 **65270 字节** |
| 2026-09-19 | v0.75 | 03-7 **首轮批改**（三道证明全对；题 8 一处待补） | **三道证明全对**（exit 0、零 warning、无 `sorry`）：① `have hq :q := h.right` + `show … from ⟨h.left, hr hq⟩`；② **与参考解不同但更好**——把内层打包成一个中间事实 `have hrq : r ∧ q := ⟨h.right.right, h.right.left⟩`，**只用一个 `have`**、顺序 `r` 在前正确；③ **`suffices` 用对了**：`from` 后面写的是「有了 `hqr` 怎么做出目标」、第二行才证 `hqr`——**最易写反的地方没写反**。判断题 **4-6 全对**（展开式 / `let` 可用 / 两件事 + 语法错）。**⚠️ 题 7** 结论对且验证区有逐字实测，但答案没写依据出处（提醒级）。**⚠️ 题 8** 前两条都用「展示」一词笼统带过，**`have` 漏了「新增一个后面可用的假设 `h : p`**」这半句——正是题目禁止的笼统说法。**验证区 4/4 全对**（`#print u1`、`#print v1` 多行块逐字相符；`w1`/`w2` 零警告；`x1` 故意漏写 rc=1）。**工具教训（第 2 次）**：我隔离实测验证区时**又漏了 `variable (p q r : Prop)` 声明** → `u1` 假报 ❌（`Unknown identifier p`）；**隔离编译必须带上上下文里的 `variable` 行** |
| 2026-09-19 | v0.76 | 03-7 **二轮通过（8/8）**；第 3 章 3-1～3-7 全部通过 | **二轮修订**：题 7 补上**依据出处**（「`#print t1` 打出来的是带 `have` 的」——原答案只有结论、没写跑的是哪条命令）；题 8 的 `have` 补上**关键那半句**（「展示一个中间证明 `e`，**然后用它作为后续的假设**」——原答案只写「展示一个中间证明」，正是题目禁止的笼统说法）。`check.sh 03-7` → 退出码 0、零 warning、无 `sorry` → **8/8**。**可选精确化（不扣分）**：`have h : p := e` 里**成为后续假设的是名字 `h : p`（后面写 `h`），不是 `e` 本身**。**第 3 章进度 7/10 课**（3-1～3-7 全 8/8），下一站 **3-8 经典逻辑**（原文 3.5） |

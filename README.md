# Lean4Practice

![AI-driven](https://img.shields.io/badge/AI-%E4%B8%BB%E5%AF%BC%E5%BC%80%E5%8F%91-blueviolet)
![Human-written proofs](https://img.shields.io/badge/%E8%AF%81%E6%98%8E-%E4%BA%BA%E7%B1%BB%E6%89%8B%E5%86%99-lightgrey)
![Status](https://img.shields.io/badge/status-%E5%AD%A6%E4%B9%A0%E4%B8%AD-orange)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow)

> ## 🤖 一个「AI 主导、人类习作」的 Lean 学习项目
>
> 本仓库的**文档、基础设施与示范代码**由 AI 撰写与维护；
> 但 **Lean 习题的证明由人类亲自完成**——这是**刻意的设计**，不是遗漏。
>
> 人类负责三件事：**提出需求、拍板决策、写练习题**。AI 负责其余全部：讲解、示范、批改、文档，
> 以及自行维护项目约定文件。唯一不由 AI 做主的是 **git 提交**——提交与推送一律由人类主动发起。
>
> 请把本仓库当作一份 **AI 协作学习的完整记录**来阅读：它同时是一份项目文档，和一个人的练习本。

---

## 这是什么

**Lean4Practice** 是一个 **Lean 4 学习与实践**项目。

它目前处于**学习进行中**的阶段：已建立**最小可编译基线**（Lean 4 + Lake，`lake build` 通过），并正在跟练教材 **[《Theorem Proving in Lean 4》](https://leanprover.github.io/theorem_proving_in_lean4/)**（TPIL）——**从第 1 章起，一节都不跳**。

> 📌 **为什么从第 1 章开始**：最初曾按「第 1 章是介绍、第 2 章偏理论」的判断直接从第 3 章入手，结果是**完全看不懂**——因为 `Prop` 与 `Type` 的区别、`#check` 输出怎么读这些基本功**全在第 2 章**。这次失败已记入 [`AGENTS.md`](./AGENTS.md) D9。**教训：看起来「只是理论」的章节不等于可以跳过。**

> ✅ **现状提示**：Lean 工具链（**`v4.34.0`**）已安装，`lake build` 已**实测通过**，构建缓存通过符号链接放在 Linux 侧。当前**有意不引入 mathlib**（TPIL 前几章不需要它）。详见 [`AGENTS.md`](./AGENTS.md) 的 D7。

学习进度、讲义与练习入口见 [`TPIL/进度.md`](./TPIL/进度.md)；完整路线图见 [`TPIL/学习计划.md`](./TPIL/学习计划.md)。

## 「AI 主导、人类习作」意味着什么

这不只是一句口号，而是一条会影响你如何使用本仓库的**实质性声明**。

### 分工

| 角色 | 负责 |
|---|---|
| **人类** | 提出需求、拍板决策、**完成 Lean 习题的证明**、发起 git 提交 |
| **AI** | 讲解与批改、示范代码与文档的撰写、自检、**自行维护项目约定文件** |

为什么不让 AI 把证明也写了？因为 **Lean 的学习恰恰发生在「写不出来」的那一刻**。如果 AI 把 `induction` 该用哪一步都填好，学习者就跳过了真正长出能力的过程。所以本仓库刻意把**习题**留给人类：AI 给示范、给提示、给批改，但不代做。

**git 是另一处边界**：**提交与推送一律由人类主动发起**。AI 可以随时修改工作区文件，但**不会自行提交**——它只在改动完成后列出待提交内容，由人类决定何时提交、提交什么。

### 由此产生的两点须知

1. **AI 产出的部分可能出错，请保持审慎。**
   本仓库的**文档与示范代码**由语言模型生成，可能存在事实错误、逻辑漏洞或过度的自信表述。**「AI 说已验证」不等于「真的验证过」**——尽管本项目已明确要求 AI 区分「已实测事实 / 推测 / 待确认」，但这条要求本身也是靠自觉执行的。
   （`TPIL/` 下由人类完成的习题不在此列——那些证明的正确性以 Lean 编译器为准。）

2. **项目约定是自我维护的。**
   本项目的规则不是人类预先写好的教条，而是 AI 在实践中**自己沉淀并持续回写**的结果。参见下方的 `AGENTS.md`。

## 项目约定入口：AGENTS.md

[`AGENTS.md`](./AGENTS.md) 是本项目的**长期记忆与协作契约**，也是**唯一**的约定入口。

它存在的目的：**即使历史会话被删除、或换到全新会话、新模型，只要读这一个文件，就能按同样的方式接续开发，不必重新摸索，也不该重复犯同样的错误。**

它记录了：

- **环境事实**——经实测验证的本机约束（勿重新摸索）
- **关键决策**——ADR 风格的决策记录及理由
- **踩坑清单**——每条都附可执行的对策
- **维护协议**——AI 在何种时机、以何种方式更新该文件
- **待定事项**——明确标注「尚未确定」，禁止凭空假设

> 该文件由 AI 主动维护：形成新工作流、被用户纠正、踩到新坑、环境变化、决策落地等时机都会触发更新，并留有变更记录。

## 如何接续开发

新会话（无论换模型还是换工具）请按此顺序开始：

1. **先读 [`AGENTS.md`](./AGENTS.md)**，再动手。
2. **再读 [`TPIL/进度.md`](./TPIL/进度.md)**，了解学习进行到哪一章、有哪些卡点。
3. **遵守其中的环境约束**——尤其「不运行无缓存的全量构建」这条硬约束（本机仅 2 核 / 3.8 GiB 内存）。
4. **遇到 `AGENTS.md` §7 标注的「待形成」事项，先与人类确认**，不要自行当作既定规则。
5. **⚠️ 不要替人类做习题**——讲解、示范、批改都可以，**代做不行**（见上文「分工」）。

## 本地构建与检查

```bash
export PATH="$HOME/.elan/bin:$PATH"   # elan 不会自动进入非登录 shell 的 PATH
lake build                            # 构建库：首次约 25 秒，增量接近瞬时
bash TPIL/check.sh 02-2               # 检查某一课的示例与习题（课号是纯 ASCII）
```

习题**不属于构建库**，所以 `lake build` 不会检查它们，必须单独检查。`TPIL/check.sh` 会自动分辨两类文件并给出结论：

- **示例** → 要求零警告零错误
- **习题** → 统计「整行就是 `sorry`」的占位：`0` 处即**验收通过**，否则提示还剩几处

> 💡 **为什么要用脚本**：课文件名含中文，手敲容易字符错位（实测踩过）。课号是纯 ASCII，用它就不必碰中文文件名。不带参数运行会列出所有可用课号。

> ⚠️ `sorry` 只是 **warning**，退出码仍是 **0**——「能通过」**不代表**做完了。完整验收命令见 [`TPIL/进度.md`](./TPIL/进度.md)。

构建产物通过符号链接落在 Linux 侧，不占用 Windows 盘。

## 仓库结构

当前**实际存在**的文件（可编译通过）：

```
Lean4Practice/
├── README.md            # 本文件
├── AGENTS.md            # 项目长期记忆与协作契约（唯一约定入口）
├── LICENSE              # MIT 许可证全文
├── lean-toolchain       # 固定的 Lean 版本：v4.34.0
├── lakefile.toml        # Lake 项目定义
├── lake-manifest.json   # 依赖锁定（当前无依赖）
├── Lean4Practice.lean   # 库根模块，负责 import 各子模块
├── Lean4Practice/       # 源码目录
│   └── Basic.lean       # 冒烟测试：几个 trivial 证明
├── TPIL/                # 学习目录（不属于构建库，逐文件检查）
│   ├── 学习计划.md       # 学习路线图
│   ├── 进度.md           # 当前进度、卡点、检查命令（新会话先读）
│   ├── check.sh          # 检查脚本：bash TPIL/check.sh <课号>
│   ├── 01-1-讲义.md      # 第 1 章讲义
│   ├── 02-1-讲义.md      # 第 2 章第 1 课讲义
│   ├── 02-1-示例.lean    # 示范示例（AI 写，已通过编译且零警告）
│   ├── 02-1-习题.lean    # 习题（【人类作答】）
│   ├── 02-2-讲义.md      # 第 2 章第 2 课讲义
│   ├── 02-2-示例.lean    # 示范示例（AI 写，已通过编译且零警告）
│   ├── 02-2-习题.lean    # 习题（【人类作答】）
│   └── 03-*.md / *.lean  # 旧材料，暂不启用（待学到第 3 章时重做）
├── .gitattributes       # 文本固定 LF、二进制显式标记
├── .gitignore           # 忽略 .lake 符号链接与构建产物
├── .lake -> Linux 侧构建缓存   # 符号链接，本机专属，已被忽略
└── .git/                # 本地仓库
```

> 注：`.lake` 在本项目中是一个**符号链接**，指向 Linux 侧的构建缓存（源码在 Windows 挂载盘上，而该盘读写慢且权限位无效，故构建缓存必须外置）。它只在本机成立，**已被 gitignore，绝不能提交**。
> 注：新增 `.lean` 文件后，需要**在 `Lean4Practice.lean` 中加对应的 `import`**，否则 `lake build` 不会编译它。

## 技术环境与已知约束

| 项 | 事实 |
|---|---|
| 平台 | WSL2 + Ubuntu 22.04.5 LTS |
| 路径 | `/mnt/e/DSHSpace/Lean4Practice`（Windows `E:` 盘挂载） |
| 挂载 | `9p`/drvfs：**大小写不敏感、权限位无效** |
| 资源 | **2 核 / 3.8 GiB 内存**（从源码构建 mathlib 基本不可行） |
| 工具链 | **已安装**：elan `4.2.4` + Lean `v4.34.0`（见 `AGENTS.md` D7） |
| 网络 | 可直连 GitHub；不稳定时可用宿主机代理兜底（端口 `10808`，见 `AGENTS.md` §6.14） |
| 版本控制 | git，分支 `main`，remote 走 SSH |

这些约束不只是环境描述，它们直接决定了若干设计取舍（例如为何构建缓存要外置）。**完整清单与对策见 [`AGENTS.md`](./AGENTS.md) §2 与 §6。**

## 尚未确定的事项

以下内容仍在 `AGENTS.md` §7 中标注为「待形成」，**在此之前不应被当作既定规则**：

- ~~项目主线形态~~ → **已定为跟练 TPIL，自第 1 章起、一节不跳**，见 `AGENTS.md` D9
- 是否引入 mathlib（当前**有意不引入**；TPIL 后续章节可能需要）
- 命名约定（文件 / 模块 / 定理）
- 证明风格与 `sorry` 的使用边界
- 提交粒度规范

## 许可

本项目采用 **[MIT 许可证](./LICENSE)**。

```
MIT License
Copyright (c) 2026 chaoskey
```

> **关于版权归属的说明**：本仓库由 AI 与人类共同产出，版权归属于创建并维护该仓库的自然人（GitHub 账号 [@chaoskey](https://github.com/chaoskey)）——在多数司法辖区，AI 本身无法成为著作权主体，而人类习作部分本就由该自然人创作。此处仅为事实性说明，不构成法律意见。

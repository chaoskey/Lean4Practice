# Lean4Practice

![Fully AI-generated](https://img.shields.io/badge/Fully_AI--generated-100%25-blueviolet)
![Human role](https://img.shields.io/badge/human-%E5%8F%AA%E8%B4%9F%E8%B4%A3%E4%BA%A4%E6%B5%81%E4%B8%8E%E6%8B%8D%E6%9D%BF-lightgrey)
![Status](https://img.shields.io/badge/status-%E6%97%A9%E6%9C%9F%E6%90%AD%E5%BB%BA%E4%B8%AD-orange)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow)

> ## 🤖 这是一个「完全由 AI 开发」的项目
>
> 本仓库的**全部内容**——代码、文档、提交信息、以及你现在读的这份 README——**均由 AI 撰写、提交并推送**。
> 人类参与者的角色只有两项：**提出需求**与**拍板决策**。没有任何一行内容由人类手写输入。
>
> 请把本仓库当作一份 **AI 协作开发的完整记录**来阅读，而**不是**一份经人工逐字精校的产物。

---

## 这是什么

**Lean4Practice** 是一个 **Lean 4 学习与实践**项目。

它目前处于**最早期阶段**：已建立**最小可编译基线**（Lean 4 + Lake，`lake build` 通过），但还没有任何实质练习内容。项目的具体形态——是跟练教材习题、做数学形式化、还是自建 Lean 库——**有意先不定死**，由实际进展决定。

> ✅ **现状提示**：Lean 工具链（**`v4.34.0`**）已安装，`lake build` 已**实测通过**，构建缓存通过符号链接放在 Linux 侧。当前**有意不引入 mathlib**。详见 [`AGENTS.md`](./AGENTS.md) 的 D7。

## 「完全由 AI 开发」意味着什么

这不只是一句口号，而是一条会影响你如何使用本仓库的**实质性声明**。

### 分工

| 角色 | 负责 |
|---|---|
| **人类** | 提出需求、拍板决策、纠正方向 |
| **AI** | 全部文档与代码的撰写、git 提交与推送、自检、以及**自行维护项目约定文件** |

人类不会亲自敲代码或写文档，也不会替 AI 检查格式；AI 则不应把「需要人类补写」当作方案。

### 由此产生的两点须知

1. **AI 的输出可能出错，请保持审慎。**
   本仓库的内容由语言模型生成，可能存在事实错误、逻辑漏洞或过度的自信表述。**「AI 说已验证」不等于「真的验证过」**——尽管本项目已明确要求 AI 区分「已实测事实 / 推测 / 待确认」，但这条要求本身也是靠自觉执行的。

2. **项目约定是自我维护的。**
   本项目的规则不是人类预先写好的教条，而是 AI 在实践中**自己沉淀并持续回写**的结果。参见下方的 `AGENTS.md`。

## 项目约定入口：AGENTS.md

[`AGENTS.md`](./AGENTS.md) 是本项目的**长期记忆与协作契约**，也是**唯一**的约定入口。

它存在的目的：**即使历史会话被删除、或换到全新会话、新模型，只要读这一个文件，就能按同样的方式接续开发，不必重新摸索，也不该重复犯同样的错误。**

它记录了：

- **环境事实**——经实测验证的本机约束（勿重新摸索）
- **关键决策**——ADR 风格的决策记录及理由
- **踩坑清单**——每条都附可执行的对策
- **维护协议**——AI 在何种时机、以何种方式更新该文件
- **待定事项**——明确标注「尚未确定」，禁止凭空假设

> 该文件由 AI 主动维护：形成新工作流、被用户纠正、踩到新坑、环境变化、决策落地等时机都会触发更新，并留有变更记录。

## 如何接续开发

新会话（无论换模型还是换工具）请按此顺序开始：

1. **先读 [`AGENTS.md`](./AGENTS.md)**，再动手。
2. **遵守其中的环境约束**——尤其「不运行无缓存的全量构建」这条硬约束（本机仅 2 核 / 3.8 GiB 内存）。
3. **遇到 `AGENTS.md` §7 标注的「待形成」事项，先与人类确认**，不要自行当作既定规则。

## 本地构建

```bash
export PATH="$HOME/.elan/bin:$PATH"   # elan 不会自动进入非登录 shell 的 PATH
lake build
```

首次构建约 **25 秒**，增量构建接近瞬时。构建产物通过符号链接落在 Linux 侧，不占用 Windows 盘。

## 仓库结构

当前**实际存在**的文件（可编译通过）：

```
Lean4Practice/
├── README.md            # 本文件
├── AGENTS.md            # 项目长期记忆与协作契约（唯一约定入口）
├── LICENSE              # MIT 许可证全文
├── lean-toolchain       # 固定的 Lean 版本：v4.34.0
├── lakefile.toml        # Lake 项目定义
├── lake-manifest.json   # 依赖锁定（当前无依赖）
├── Lean4Practice.lean   # 库根模块，负责 import 各子模块
├── Lean4Practice/       # 源码目录
│   └── Basic.lean       # 冒烟测试：几个 trivial 证明
├── .gitattributes       # 文本固定 LF、二进制显式标记
├── .gitignore           # 忽略 .lake 符号链接与构建产物
├── .lake -> Linux 侧构建缓存   # 符号链接，本机专属，已被忽略
└── .git/                # 本地仓库
```

> 注：`.lake` 在本项目中是一个**符号链接**，指向 Linux 侧的构建缓存（源码在 Windows 挂载盘上，而该盘读写慢且权限位无效，故构建缓存必须外置）。它只在本机成立，**已被 gitignore，绝不能提交**。
> 注：新增 `.lean` 文件后，需要**在 `Lean4Practice.lean` 中加对应的 `import`**，否则 `lake build` 不会编译它。

## 技术环境与已知约束

| 项 | 事实 |
|---|---|
| 平台 | WSL2 + Ubuntu 22.04.5 LTS |
| 路径 | `/mnt/e/DSHSpace/Lean4Practice`（Windows `E:` 盘挂载） |
| 挂载 | `9p`/drvfs：**大小写不敏感、权限位无效** |
| 资源 | **2 核 / 3.8 GiB 内存**（从源码构建 mathlib 基本不可行） |
| 工具链 | **已安装**：elan `4.2.4` + Lean `v4.34.0`（见 `AGENTS.md` D7） |
| 网络 | 可直连 GitHub；不稳定时可用宿主机代理兜底（端口 `10808`，见 `AGENTS.md` §6.14） |
| 版本控制 | git，分支 `main`，remote 走 SSH |

这些约束不只是环境描述，它们直接决定了若干设计取舍（例如为何构建缓存要外置）。**完整清单与对策见 [`AGENTS.md`](./AGENTS.md) §2 与 §6。**

## 尚未确定的事项

以下内容仍在 `AGENTS.md` §7 中标注为「待形成」，**在此之前不应被当作既定规则**：

- 项目主线形态与目录组织
- 是否引入 mathlib（当前**有意不引入**）
- 命名约定（文件 / 模块 / 定理）
- 证明风格与 `sorry` 的使用边界
- 提交粒度规范

## 许可

本项目采用 **[MIT 许可证](./LICENSE)**。

```
MIT License
Copyright (c) 2026 chaoskey
```

> **关于版权归属的说明**：本仓库内容虽然全部由 AI 生成，但版权归属于创建并维护该仓库的自然人（GitHub 账号 [@chaoskey](https://github.com/chaoskey)）——在多数司法辖区，AI 本身无法成为著作权主体。此处仅为事实性说明，不构成法律意见。

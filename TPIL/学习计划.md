# TPIL 学习计划

> **2026-09-17 重新规划**：原计划从第 3 章开始，实践证明**跳步了**。现改为**从第 1 章开始，一节都不跳**。

---

## 一、为什么之前失败了（这条教训要留住）

**我的原判断**：「第 1 章是介绍、第 2 章偏理论，直接从第 3 章开始写证明更高效。」

**这个判断是错的。** 你在第 3 章卡住的概念——`Prop` 和 `Type` 有什么区别、`Nat` 到底是什么、`#check` 输出怎么读——**全部在第 2 章 2.1 和 2.2 里讲**。我让你跳过了整整一章地基，然后在第 3 章的地板上要求你走。

**教训：**「这一章看起来只是理论」**不等于**「可以跳过」。第 2 章是全书的地基，第 3 章只是在它上面盖房子。

---

## 二、总路线图

| 阶段 | 教材 | 课数 | 状态 |
|---|---|---|---|
| 0 | 环境安装 | — | ✅ 已完成（跳过） |
| **1** | **第 1 章 Introduction** | **1 课** | ✅ **已完成** |
| **2** | **第 2 章 Dependent Type Theory** | **10 课** | ✅ **已完成（10/10 全部满分）** |
| **3** | **第 3 章 Propositions and Proofs** | **10 课** | 🟡 **当前**（3-1 材料已发；旧材料需重做，见 §五） |
| 4 | 第 4 章 Quantifiers and Equality | 待定 | ⬜ |
| 5 | 第 5 章 Tactics | 待定 | ⬜ |
| 6 | 第 6 章 Interacting with Lean | 待定 | ⬜ |
| 7 | 第 7 章 Inductive Types | 待定 | ⬜ |
| 8 | 第 8 章 Induction and Recursion | 待定 | ⬜ |
| 9–12 | Structures / Type Classes / Conv / Axioms | 待定 | ⬜ |

> 「课数」是初估，**可以随时调整**。宁可把一课拆成两课，也不要把两课压成一课。

---

## 三、第 2 章拆成 10 课（重点章）

> **课号按「课序」排，不强制与小节一一对应**——一节太长就拆成两课，两节太短也可能并成一课。以实际好懂为准。

| 课 | 对应小节 | 主题 | 为什么需要它 |
|---|---|---|---|
| 2-1 | 2.1 前半 | 每个表达式都有类型；`def`、`#check`、`#eval`、注释 | 最基本的词汇 + 最常用的三个工具 |
| 2-2 | 2.1 后半 | 从已有类型造新类型：`→`、`×`；Unicode 写法 | 后面到处都在用这两种构造 |
| **2-3** | **2.2** | **`Prop` vs `Type`**；类型本身也是对象（含 `Type : Type 1` 的层级） | ← **你上次卡住的地方，就是这一节** |
| 2-4 | 2.3 | 函数抽象与求值：`fun`、应用、`#eval` | 后面**所有证明都是函数**，这课是核心 |
| 2-5 | 2.4 | 定义：`def` | 给东西起名字 |
| 2-6 | 2.5 | 局部定义：**`let`**（只讲 `let`，见下方注） | 在表达式内部给中间结果起名字；证明里会一直用到 |
| 2-7 | 2.6 | 变量与区段：`variable` / `section`（**含 `variable (α : Type)` 的演示**，见下方注） | 读懂 Lean 代码的组织方式；参数只声明一次的机制 |
| 2-8 | 2.7 | 命名空间：`namespace` | 读懂标准库的名字 |
| 2-9 | 2.8 | 依赖类型 | 为什么 `Fin n` 能当类型 |
| 2-10 | 2.9 | 隐式参数 `{α : Type}` | 为什么有些参数不用写 |

> **原本是 9 课、与小节 1:1 对应；现在拆成 10 课。** 原因见 §七 原则 2——2.1 一节里塞了两类完全不同的东西（工具 vs 概念），一次讲完等于又犯「一课塞太多」的老毛病。

> ⚠️ **一处「待安排」**：原文 2.2 里还含**宇宙多态**（`universe u`、`Type u`、`List.{u}`、`max u v`）。
> 它**不属于 2-3**（2-3 只讲「类型也有类型」与 `Prop`/`Type` 两个宇宙，已明确标注「本课不引入」），
> 也还没有归属的课。**待学到真正需要它的地方再安排**（多半与 2-9 依赖类型 / 2-10 隐式参数一带有关）。
> 在那之前，讲义与示例里出现 `. {u}` / `Type u` 时都会明确标注「本课不引入」。

> ⚠️ **一处范围修正（2026-09-19，开 2-6 前核对原文后改的）**：本表原把 2-6 写成「`let` / `have` / `show`」，
> 那是**助手的旧猜测**，不是原书的安排。核对官网原文（2.5 Local Definitions）后确认：
> **2.5 一节只讲 `let`**；**`have` 在原文 3.4「引入辅助子目标」（Introducing Auxiliary Subgoals）**，
> `show` 也与证明目标有关——**两者都属于第 3 章《命题与证明》**（它们离不开 `=` / `rfl` / `by` 那套写法）。
> 按「一课只引入一个新概念」（§七 原则 2）与 `AGENTS.md` **D14 第 3 条**，**2-6 只讲 `let`**，
> `have` / `show` 留到第 3 章。
> **教训（与 D9 的「跳步」同源）：课表只是估计；开课前必须核对原文，不能凭印象排课。**

> ⚠️ **一处范围修正（2026-09-19，开 2-7 前核对原文后改的）**：原文 2.6 的例子用 **`variable (α β γ : Type)`**
> 把「类型」也变成变量，而我在 **02-5 讲义**里曾把「把类型当参数」整个推到 **2-9**。现在**修正为**：
> - **2-7 正式演示这种写法，要求「能读懂、能照用」**（`α : Type` 就是「`α` 是一个类型」，这在 02-3 学过）；
> - 但「**为什么后面的参数可以依赖前面的参数**」（`g : β → γ` 里的 `β`、`γ` 本身就是参数）属于**依赖函数类型**，
>   **原理仍归 2-9**；2-7 的**习题不出**需要自己设计类型参数的题。
>
> 理由：原文 2.6 正是用类型变量来演示 `variable` 的；完全避开它，这一课就只剩「具体类型」的空壳，
> 反而要等到 2-9 再重学一遍。**标注清楚「演示 / 原理」的分界，比整段推迟更好。**

---

## 三·B、第 3 章拆成 10 课（2026-09-19 核对原文后定的）

> 原文第 3 章共 **7 节**（3.3 下还有 4 个子节）。按「一课只引入一个新概念」（§七 原则 2）拆成 **10 课**：

| 课 | 对应小节 | 主题 | 为什么需要它 |
|---|---|---|---|
| **3-1** | **3.1** | **命题即类型**（`And`/`Or`/`Not` 的本名、`→` 的双重身份、Curry–Howard、**proof irrelevance**） | **先搞清「命题」和「证明」在 Lean 里到底是什么**；本章的地基。⚠️ 本课**不写证明** |
| 3-2 | 3.2 | **第一个证明**：`theorem`、用 `fun` 引入假设、`show … from …`、`#print`、把定理当函数用 | **你第一次真正写证明** |
| 3-3 | 3.3.1 | **合取 `∧`**：`And.intro`、`And.left`/`And.right`（`.left`/`.right`/`.1`/`.2`）、`⟨,⟩`、`example`、点记号 `e.bar` | 最常用的连接词。⚠️ **已核对原文**：3.3.1 **全部用证明项**，**不含任何 tactic**——`cases`/`constructor` 是 tactic（第 5 章），本课**不引入** |
| 3-4 | 3.3.2 | **析取 `∨`**：`Or.intro_left`/`Or.intro_right`（短写 `Or.inl`/`Or.inr`）、`Or.elim`（`h.elim`）分情况 | 需要分情况讨论。✅ **材料已发（2026-09-19）**；⚠️ **已核对原文**：3.3.2 同样用证明项（`Or.elim` 接三个参数），**不是** `cases`/`apply` |
| 3-5 | 3.3.3 | **否定与假**：`False`、`¬p`、`absurd`、`exfalso`、`Contradiction` | 反证的基础 | ✅ **材料已发（2026-09-19）**
| 3-6 | 3.3.4 | **逻辑等价 `↔`**：`Iff.intro`、`.mp`/`.mpr`、用 `↔` 改写 | 把两个命题「绑」在一起用 |
| 3-7 | 3.4 | **引入辅助子目标**：`have`、`suffices`、`show` | 长证明的组织方式（**02-6 讲过「`have` 留到第 3 章」，就是这里**） |
| 3-8 | 3.5 | **经典逻辑**：`Classical`、`byContradiction`、`em`、`by_cases` | 非构造性的推理方式 |
| 3-9 | 3.6 | **命题有效性的例子**（综合演练） | 把前面所有工具串起来 |
| 3-10 | 3.7 + 补充 | **综合习题**（含原书 3.7 的练习） | 第 3 章收官 |

> ⚠️ **课表是估计，开课前必须核对原文**（这条教训见 §三 的两处「范围修正」）。
> 3-1 已核对：原文 3.1 **纯概念**（可写的证明在 3.2），所以 3-1 的动手部分放在**类型层级**（写 `Prop`），
> 真正的「第一个证明」留给 **3-2**。

> ⚠️ **原文 3.1 里我明确不引入的东西**（讲义 §9 逐条标注）：`structure`（第 7 章）、`axiom`（危险）、
> `Sort`/`universe`/`Type u`（宇宙多态）、以及那个「`Proof p` 结构」的反面方案——**只转述，不要求会写**。

## 四、每一课的固定流程

1. 你读 `NN-K-讲义.md` —— **自包含中文讲解，不需要读英文原文**
2. 你做 `NN-K-习题.lean` —— **由你亲自完成**
3. 我跑验收命令**批改**，指出对在哪、绕在哪、有没有更直接的写法
4. 卡住就说「第几题、卡在哪一步、试过什么」，我给**提示**，不直接给答案

> 📌 **讲义自包含**（见 `AGENTS.md` D11）：用户读英文原文吃力，所以**只读讲义就应能完整学会本课**。
> 讲义开头仍会附原文链接，但那只是「想深挖时的延伸阅读」，**不是理解本课的前提**。

**无代码的课**（如第 1 章）只有讲义 + 几道口头思考题，没有 `.lean` 文件。

---

## 五、目录与命名约定

```
TPIL/
├── 学习计划.md          # 本文件：路线图
├── 进度.md              # 当前位置 + 卡点（每次学习后更新）
├── 01-1-讲义.md         # 第 1 章第 1 课
├── 02-1-讲义.md   …     # 第 2 章第 1 课 …
├── 02-K-示例.lean       # 我写的示例：必须编译通过且零 warning
├── 02-K-习题.lean       # 【你作答】
└── 03-*.md / 03-*.lean  # 旧的第 3 章材料：**暂不启用**
```

> **`NN` = 章号，`K` = 课号。** 例：`02-2-讲义.md` = 第 2 章第 2 课。
>
> ⚠️ **`03-` 开头的旧文件先别管。** 那是按旧方式写的第 3 章材料，等到我们真的学到第 3 章时，我会照新方式（更慢、更细）重做一遍。

---

## 六、双方职责

| 角色 | 负责 |
|---|---|
| **你** | 读讲义、**做题**、把卡点说清楚、拍板方向 |
| **我** | 写讲义与示例（全部实测过）、批改你的作答、维护进度与计划、环境与杂务 |

---

## 七、节奏原则（从失败里总结的）

1. **不跳步。** 哪怕一节看起来「只是理论」，也先过一遍。
2. **一次只引入一个新概念。** 一课塞太多 = 白讲。
3. **凡我说的，都是实测的。** 讲义里出现的 Lean 输出，都真跑过；示例文件必须编译通过、零 warning。
4. **你卡住就是我的讲义有问题。** 不要怀疑自己——直接说「第几节看不懂」，我重写。
5. **宁可慢。** 学完 3 章比「读过 12 章」有用得多。
6. **讲义一律自包含中文。** 用户读英文原文吃力，讲义必须能独立读懂（见 `AGENTS.md` D11）。
   原文链接只作延伸阅读；**写「详见原文」而不解释内容，等于没讲**。

/-
  TPIL 第 3 章 · 第 1 部分：蕴含 `→`  —— 你的习题

  规则：把每个 `sorry` 换成真正的证明。**不要改动定理的名称和陈述。**
  题目只用到三个策略：`intro`、`exact`、`apply`。

  检查：
    export PATH="$HOME/.elan/bin:$PATH"
    lake env lean TPIL/03-习题.lean

  ⚠️ 注意：`sorry` 只是 warning，退出码是 0。
     「能通过」不代表做完了——验收命令见 TPIL/进度.md。

  卡住了先看文件末尾的「提示」，再不行就问我。
-/

/-! ## 热身 -/

/-- 习题 1：最基础的蕴含。请分别用**项模式**和**策略模式**各写一遍。 -/
theorem ex1 (p : Prop) : p → p := by
  sorry

/-! ## 练习 `intro` 与箭头顺序 -/

/-- 习题 2：注意两个前提的**顺序**——要证的是 `q → p → q`，不是 `p → q → p`。 -/
theorem ex2 (p q : Prop) : q → p → q := by
  sorry

/-! ## 练习 `apply` -/

/-- 习题 3：传递性。先把 `(p → q)` 和 `(q → r)` 用 `intro` 拿进来，再对目标 `r` 逐步回退。 -/
theorem ex3 (p q r : Prop) : (p → q) → (q → r) → p → r := by
  sorry

/-! ## 练习同一前提的复用 -/

/-- 习题 4：`q` 和 `p` 都要喂给 `h`，注意喂进去的**顺序**。 -/
theorem ex4 (p q r : Prop) : (p → q → r) → q → p → r := by
  sorry

/-- 习题 5：综合。`hp : p` 需要被用**两次**——一次给 `h1` 得到 `q`，一次和 `q` 一起给 `h2`。 -/
theorem ex5 (p q r : Prop) : (p → q) → (p → q → r) → p → r := by
  sorry

/-! ## 挑战题（可选） -/

/-- 挑战 1：`S` 组合子。这是纯蕴含逻辑里最经典的一条，只用 `apply`/`exact` 也能做出来。 -/
theorem ex6 (p q r : Prop) : (p → q → r) → (p → q) → p → r := by
  sorry

/-- 挑战 2：把习题 3 用**项模式**（`fun ... => ...`）重写一遍，不要用 `by`。 -/
theorem ex7 (p q r : Prop) : (p → q) → (q → r) → p → r :=
  sorry

/-! ------------------------------------------------------------------
    提示（卡住再看；仍然卡住就直接问我，我会逐步引导而不直接给答案）

    通用三问：
      1. 当前**目标**是什么？
      2. 有哪些**假设**？各自的类型是什么？（不确定就 `#check`）
      3. 有哪个假设的**结论**长得像目标？→ 用 `apply`
         有哪个假设**本身**就是目标？→ 用 `exact`

    习题 1：目标 `p → p`，假设里有 `hp : p`。
    习题 2：`intro` 的顺序要与箭头从左到右一致；最后 `exact` 里应是你**先**引入的那个。
    习题 3：`intro h1 h2 hp` 之后目标是 `r`。谁的前提是 `p`、结论是 `r`？
    习题 4：`h : p → q → r`。`h hp` 之后得到什么类型？（`#check h hp` 看看）
    习题 5：先 `apply h2`，目标会变成 `q`；此时 `h1` 还等着一个 `p`。
    挑战 1：先 `intro h1 h2 hp`，然后目标 `r` 能由谁给出？它还需要什么？
    挑战 2：项模式和策略模式一一对应：`intro x` ↔ `fun x =>`，`exact t` ↔ `t`。
   ------------------------------------------------------------------ -/

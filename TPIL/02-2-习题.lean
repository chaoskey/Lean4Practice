/-
  第 2 章 · 第 2 课 · 习题：从已有类型造新类型（`→` 和 `×`）

  规则：
    · 前三题：把 `sorry` 换成你的定义
    · 后四题：把**你的预测 / 判断**写在「我的答案：」后面
              —— 先自己判断，**再**运行验证。

  检查：
    lake env lean TPIL/02-2-习题.lean

  ⚠️ 本课还没学「自己写函数」（那是下一课 fun 的内容）。
     前三题请用**示例里出现过的现成函数**来填。

  ⚠️ `sorry` 要单独占一行，否则验收脚本检不出来。
-/

/- ============ 一、用 `def` 写定义（把 sorry 换掉）============ -/

/-- 题 1：声明常量 `p1`，类型 `Nat × Bool`，值是 `(1, true)`。 -/
def p1 : Nat × Bool :=
  sorry

/-- 题 2：声明常量 `f`，类型 `Nat → Nat`，让它表示「把一个自然数加一」。
    提示：示例里有一个现成的函数正好干这事。 -/
def f : Nat → Nat :=
  sorry

/-- 题 3：声明常量 `g`，类型 `Nat → Nat → Nat`，让它表示「加法」。
    提示：同样用现成的那个。 -/
def g : Nat → Nat → Nat :=
  sorry


/- ============ 二、预测 / 判断 ============ -/

/- 题 4：下面这一行会打印什么？（连 `: Type` 一起写）

      #check Nat × Nat

    我的答案： -/


/- 题 5：`Bool → Nat → Nat` 是什么意思？
    提示：箭头右结合。请写出它与哪个**带括号**的写法等价。

    我的答案： -/


/- 题 6：下面四个东西，哪些是「类型」，哪些是「值」？

      Nat × Nat        (5, 9)        Nat.succ        Bool → Nat

    我的答案： -/


/- 题 7：`Nat.add 3` 是「类型」还是「值」？它的类型是什么？
    （先想清楚：`Nat.add` 是类型还是值？喂它一个 3 之后呢？）

    我的答案： -/


/- ============ 验证区（判断完之后，取消注释来核对）============

#check Nat × Nat
#check Bool → Nat → Nat
#check Nat.add 3
#eval g 5 2
#eval (5, 9).2

-/

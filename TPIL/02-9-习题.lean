/-
  第 2 章 · 第 9 课 · 习题：依赖类型（dependent types）

  规则：
    · 前三题：把 `sorry` 换成你的定义（**类型标注都写全了**，你只写「值」那部分）
    · 后五题：把**你的预测 / 判断**写在「我的答案：」后面
              —— 先自己判断，**再**运行验证。

  检查：
    lake env lean TPIL/02-9-习题.lean

  ⚠️ 本课的题面一律用**普通注释** `/- … -/`（不是 `/-- … -/`）：
     本课虽然只在 `sorry` 处填东西，但保持和前两课一致——
     `/-- … -/` 是**文档注释**，必须**紧贴**一个声明，中间插东西会报错。
  ⚠️ `sorry` 要单独占一行，否则验收脚本检不出来。
-/

/- ============ 一、补完三个定义（把 `sorry` 换掉）============ -/

/- 题 1：补完 `zeroFin`——它返回 `Fin (n + 1)` 里**最小的那个数**。
    ⚠️ 右边**不要**写成 `fun n => …`（`n` 已经在这个定义的参数列表里了）。 -/
def zeroFin (n : Nat) : Fin (n + 1) :=
  sorry

/- 题 2：补完 `packDep`——把 `a` 和 `b` **装进一个依赖对**。
    注意返回类型是 `(a : α) × β a`（第二个分量的类型依赖第一个）。 -/
def packDep (α : Type) (β : α → Type) (a : α) (b : β a) : (a : α) × β a :=
  sorry

/- 题 3：补完 `getSnd`——取出依赖对 `p` 的**第二个分量**。
    注意返回类型是 `β p.1`：它**依赖** `p` 的第一个分量。 -/
def getSnd (α : Type) (β : α → Type) (p : (a : α) × β a) : β p.1 :=
  sorry


/- ============ 二、预测 / 判断 ============ -/

/- 题 4：`#check zeroFin` 与 `#check zeroFin 3` 分别会打印出什么？

    我的答案： -/


/- 题 5：`(a : α) → β` 里的 `β` **不依赖** `a` 的时候，
    这个类型和 `α → β` 是**同一个**东西吗？为什么？

    我的答案： -/


/- 题 6：如果 `p : (a : α) × β a`，那么 `p.2` 的**类型**是什么？
    （提示：它会不会依赖 `p` 的某个部分？）

    我的答案： -/


/- 题 7：`Fin 3` 与 `Fin 5` 是**同一个类型**吗？
    如果不是，它们的区别在哪？

    我的答案： -/


/- 题 8：原文说 `Type → α → List α → List α` 这个类型「**说不通**」。
    请说出**为什么**说不通（提示：那个 `α` 指着什么？）。

    我的答案： -/


-- ============ 验证区（先自己判断，判完再**取消注释**核对）============
--
-- ⚠️ 这里故意留成注释：否则你一跑检查脚本，结果就直接印出来了。
-- ⚠️ 带 `#eval` 的那几行要等你把前三题做完再取消注释
--    （`#eval` 不能对含 `sorry` 的东西求值）。
-- ⚠️ 本区**只给你自己核对用**，里面**不写任何预期结果**。

-- #check zeroFin
-- #check zeroFin 3
-- #eval zeroFin 3

-- #check packDep
-- #check getSnd

-- 额外核对（讲义 §4 / §5 那两个现象）：
-- #check ((a : Nat) → Nat)
-- #check (Nat → Nat)

-- 想看看 Fin 的「依赖」到底有多实在，可以自己试试下面这几行：
-- #check Fin
-- #check Fin 3
-- #check Fin 5
-- #check (2 : Fin 3)
